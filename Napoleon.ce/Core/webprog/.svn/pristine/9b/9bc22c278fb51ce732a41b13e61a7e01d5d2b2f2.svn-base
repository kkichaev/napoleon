import { onMounted } from 'vue'
import { useBackendStore } from 'src/stores/backend'
import { useRouter } from 'vue-router'

export function useGuard (){
  const router = useRouter()
  onMounted(function (){
    if (!useBackendStore().isAuth)
      router.push({ name: 'Login' })
  })
}
