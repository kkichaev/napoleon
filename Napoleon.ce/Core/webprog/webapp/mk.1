#!/usr/bin/sh

echo "FROM python:3.11-slim
RUN apt-get update \
	&& apt-get -y install libpq-dev gcc

RUN useradd ert
WORKDIR /home/ert

RUN python -m venv prog
RUN prog/bin/pip install psycopg2
" >> dockf

docker image rm python:webapp
docker build --force-rm -f dockf -t python:webapp ./
rm dockf
