<Window x:Class="Ads2017.SettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:properties="clr-namespace:Ads2017.Properties"
        xmlns:p = "clr-namespace:Ads2017.Properties"
        Title="{x:Static p:Resources.settings}" 
        Height="300" 
        Width="300"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        PreviewKeyDown="Window_PreviewKeyDown">
   <Window.Resources>
      <properties:Settings x:Key="Settings"/>
   </Window.Resources>

   <Grid>
      <Grid.RowDefinitions>
         <RowDefinition Height="*"/>
         <RowDefinition Height="30"/>
      </Grid.RowDefinitions>

      <GroupBox Grid.Row="0" 
         Header="{Binding Source={x:Static p:Resources.setting_connect}}">

         <StackPanel>
            <Label Content="{Binding Source={x:Static p:Resources.login}}"/>
            <TextBox x:Name="login" Text="{Binding Source={StaticResource ResourceKey=Settings}, 
               Path=Default.Login, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Content="{Binding Source={x:Static p:Resources.password}}"/>
            <TextBox x:Name="password" Text="{Binding Source={StaticResource ResourceKey=Settings}, 
               Path=Default.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Content="{Binding Source={x:Static p:Resources.ip}}"/>
            <TextBox x:Name="ip" Text="{Binding Source={StaticResource ResourceKey=Settings}, 
               Path=Default.IP, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Content="{Binding Source={x:Static p:Resources.port}}"/>
            <TextBox x:Name="port" Text="{Binding Source={StaticResource ResourceKey=Settings}, 
               Path=Default.Port, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
         </StackPanel>
      </GroupBox>
      <Grid
         Grid.Row="1">
         <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
         </Grid.ColumnDefinitions>

         <Button Grid.Column="0" x:Name="btnOK" Content="{Binding Source={x:Static p:Resources.ok}}" Click="OK_Window"/>
         <Button Grid.Column="1" x:Name="btnCancel" Content="{Binding Source={x:Static p:Resources.cancel}}" Click="Cancel_Click"/>
      </Grid>
   </Grid>
</Window>
