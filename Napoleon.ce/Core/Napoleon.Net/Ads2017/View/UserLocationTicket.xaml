<UserControl x:Class="Ads2017.UserLocationTicket"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Border BorderBrush="LightBlue" BorderThickness="2" Margin="0,0,0,3" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" x:Name="number" 
                       Text="{Binding Path=Number, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                       FontWeight="Bold"/>
            
            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="time" 
                       Text="{Binding Path=Time, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"/>
            
            <TextBlock Grid.Row="1" Grid.ColumnSpan="2" x:Name="agent" 
                       Text="{Binding Path=Client, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                       Padding="10,0"/>
        </Grid>
    </Border>
</UserControl>
