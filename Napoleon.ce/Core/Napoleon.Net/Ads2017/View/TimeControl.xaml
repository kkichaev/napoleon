<UserControl x:Class="Ads2017.TimeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TextBox x:Name="textBox" PreviewMouseUp="TextBoxPreviewMouseUp" IsReadOnly="True"
            HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        
        <Popup Name="Popup1" 
            PlacementTarget="{Binding ElementName=textBox}" 
            Placement="Bottom"
            Height="100"
            Width="{Binding ElementName=textBox, Path=ActualWidth}"
            Opened="Popup1_Opened">

            <ListView x:Name="listBox" ItemsSource="{Binding}" ItemStringFormat="{}{0:hh}:{0:mm}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="ListViewItem_PreviewMouseLeftButtonDown" />
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Popup>
    </Grid>
</UserControl>
