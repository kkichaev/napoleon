<UserControl x:Class="Ads2017.TaskPointTicket"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" 
             d:DesignWidth="300">
   <Border BorderBrush="LightBlue" BorderThickness="2" Margin="0,0,0,3">
      <Grid>
         <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
         </Grid.RowDefinitions>

         <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
         </Grid.ColumnDefinitions>

         <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
               <ColumnDefinition Width="20"/>
               <ColumnDefinition Width="*"/>
               <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" x:Name="number" 
                      Text="{Binding Path=Number, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                      FontWeight="Bold"/>
            <TextBlock Grid.Column="1" x:Name="timeplan" 
                      Text="{Binding Path=TimePlan, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"/>
            <TextBlock Grid.Column="2" x:Name="timefact" 
                      Text="{Binding Path=TimeFact, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"/>
         </Grid>
         <TextBlock Grid.Row="1" Grid.ColumnSpan="3" x:Name="client" 
                   Text="{Binding Path=Client, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                   Padding="10,0"/>
         <TextBlock Grid.Row="2" Grid.ColumnSpan="3" x:Name="address" 
                   Text="{Binding Path=Address, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                   Padding="10,0"
                   FontStyle="Italic"/>
         <TextBlock Grid.Row="13" Grid.ColumnSpan="3" x:Name="factAddress" 
                   Text="{Binding Path=FactAddress, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                   Padding="10,0"/>
      </Grid>
   </Border>
</UserControl>
