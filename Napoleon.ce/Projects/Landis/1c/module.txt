
Функция Версия() Экспорт
	Возврат "1.24";
КонецФункции

Функция Информация() Экспорт
	Возврат "Обмен информацией с АСМТ Наполеон для КА 2";
КонецФункции

Функция СведенияОВнешнейОбработке() Экспорт
	
    ПараметрыРегистрации = ДополнительныеОтчетыИОбработки.СведенияОВнешнейОбработке();

	ПараметрыРегистрации.Вид = ДополнительныеОтчетыИОбработкиКлиентСервер.ВидОбработкиДополнительнаяОбработка();
    ПараметрыРегистрации.Наименование = "Обмен данными с АСМТ Наполеон";
    ПараметрыРегистрации.БезопасныйРежим = Ложь;
    ПараметрыРегистрации.Версия = Версия();    
    ПараметрыРегистрации.Информация = Информация();
	
    ДобавитьКоманду(ПараметрыРегистрации.Команды, "Выгрузка основных данных", "ВыгрузкаОсновныхДанных", 
		ДополнительныеОтчетыИОбработкиКлиентСервер.ТипКомандыВызовСерверногоМетода());
    
    ДобавитьКоманду(ПараметрыРегистрации.Команды, "Выгрузка долгов", "ВыгрузкаДолгов", 
		ДополнительныеОтчетыИОбработкиКлиентСервер.ТипКомандыВызовСерверногоМетода());

    ДобавитьКоманду(ПараметрыРегистрации.Команды, "Загрузка документов", "ЗагрузкаДокументов", 
		ДополнительныеОтчетыИОбработкиКлиентСервер.ТипКомандыВызовСерверногоМетода());
			
	Возврат ПараметрыРегистрации;
КонецФункции

Процедура ДобавитьКоманду(ТаблицаКоманд, Представление, Идентификатор, Использование, ПоказыватьОповещение = Ложь, Модификатор = "")
  НоваяКоманда = ТаблицаКоманд.Добавить();
  НоваяКоманда.Представление = Представление;
  НоваяКоманда.Идентификатор = Идентификатор;
  НоваяКоманда.Использование = Использование;
  НоваяКоманда.ПоказыватьОповещение = ПоказыватьОповещение;
  НоваяКоманда.Модификатор = Модификатор;

КонецПроцедуры

Процедура ВыполнитьФоновоеЗадание(ИдентификаторКоманды, Хранилище) Экспорт
	ПараметрыВыполнения = Новый Структура("РезультатВыполнения", Новый Структура);
	ВыполнитьКоманду(ИдентификаторКоманды, ПараметрыВыполнения);
	Если Хранилище <> Неопределено Тогда
		ПоместитьВоВременноеХранилище(ПараметрыВыполнения.РезультатВыполнения, Хранилище);
	КонецЕсли;
КонецПроцедуры

Процедура ВыполнитьКоманду(ИдентификаторКоманды, ПараметрыВыполнения = Неопределено) Экспорт
	ЗагрузитьНастройки();
	Если ИдентификаторКоманды = "ВыгрузкаОсновныхДанных" Тогда
		ВыгрузкаОснДанных();
	ИначеЕсли ИдентификаторКоманды = "ВыгрузкаДолгов" Тогда
		ВыгрузкаДолгов();
	ИначеЕсли ИдентификаторКоманды = "ЗагрузкаДокументов" Тогда
		ПриемДокументов();
	КонецЕсли;
КонецПроцедуры

Функция КодОбъекта(эл)
	тз = ТипЗнч(эл);
	Если тз = Тип("СправочникСсылка.СоглашенияСКлиентами")
		или тз = Тип("СправочникСсылка.Пользователи")
		или тз = Тип("СправочникСсылка.ДоговорыКонтрагентов")
	Тогда
		Возврат Строка(эл.УникальныйИдентификатор());
	КонецЕсли;
	
	Возврат СокрЛП(эл.Код);
КонецФункции

Функция ПолучитьОбъект(эл, код)
	тз = ТипЗнч(эл);
	Если тз = Тип("СправочникМенеджер.СоглашенияСКлиентами")
		или тз = Тип("СправочникМенеджер.Пользователи")
		или тз = Тип("СправочникМенеджер.ДоговорыКонтрагентов")
	Тогда
		Попытка
			уид = Новый УникальныйИдентификатор(СокрЛП(код));
			Возврат эл.ПолучитьСсылку(уид);
		Исключение
			Возврат эл.ПустаяСсылка();
		КонецПопытки;
	КонецЕсли;
	
	Возврат эл.НайтиПоКоду(СокрЛП(код));
КонецФункции

Функция ЗагрузитьАгентов()
	агенты = новый ТаблицаЗначений;
	агенты.Колонки.Добавить("Код", Новый ОписаниеТипов("Строка"));
	агенты.Колонки.Добавить("Наименование", Новый ОписаниеТипов("Строка"));
	агенты.Колонки.Добавить("Логин", Новый ОписаниеТипов("Строка"));
	агенты.Колонки.Добавить("Пароль", Новый ОписаниеТипов("Строка"));
	
	ИмяФайла = ПапкаОбмена + "\USERS.DBF";
	ф = Новый Файл(ИмяФайла);
	Если ф.Существует() Тогда
		дбф = Новый XBase;
		дбф.ОткрытьФайл(ИмяФайла);
		дбф.Кодировка = КодировкаXBase.OEM;
		
		ДБФ.Первая();
		Пока ДБФ.ВКонце() = 0 Цикл
			стр = агенты.Добавить();
			стр.Код = СокрЛП(ДБФ.ID);
			стр.Наименование = СокрЛП(ДБФ.NAME);
			стр.Логин = СокрЛП(ДБФ.LOGIN);
			стр.Пароль = СокрЛП(ДБФ.PASSWORD);
			ДБФ.Следующая();
		КонецЦикла;
		
		ДБФ.ЗакрытьФайл();
	КонецЕсли;
	
	Возврат агенты;
КонецФункции

Функция ДобавитьАгента(Агенты, Агент)
	Код = КодОбъекта(Агент);
	
	Для Каждого стр Из Агенты Цикл	
		Если стр.Код = Код Тогда
			стр.Наименование = Агент.Наименование;
			Возврат Код;	
		КонецЕсли;
	КонецЦикла;
	
	стр = Агенты.Добавить();
	стр.Код = Код;
	стр.Наименование = Агент.Наименование;
	Возврат Код;	
КонецФункции

Процедура ЗаписатьАгентов(агенты)
	имяФайла =  ПапкаОбмена + "\USERS.dbf";
	                                  
	ДБФ = Новый XBase();
	ДБФ.Поля.Добавить("ID",   "S", 100, 0);
	ДБФ.Поля.Добавить("NAME", "S", 100, 0);
	ДБФ.Поля.Добавить("LOGIN", "S", 30, 0);
	ДБФ.Поля.Добавить("PASSWORD", "S", 30, 0);
	ДБФ.СоздатьФайл(имяФайла);
	ДБФ.Кодировка = КодировкаXBase.OEM;
	
	
	Для Каждого стр из агенты Цикл
		ДБФ.Добавить();
		
		ДБФ.ID = стр.Код;
		ДБФ.NAME = стр.Наименование;
		ДБФ.LOGIN = стр.Логин;
		ДБФ.PASSWORD = стр.Пароль;
		ДБФ.Записать();
	КонецЦикла;
	
	ДБФ.ЗакрытьФайл();
КонецПроцедуры

Функция СоздатьБазуПартнеров(имяВрФайла)
	имяФайла = ПапкаОбмена + "\" + имяВрФайла;
	ДБФ = Новый XBase();

	ДБФ.Поля.Добавить("ID",     "S", 55, 0);
	ДБФ.Поля.Добавить("NAME",   "S", 50, 0);
	ДБФ.Поля.Добавить("ADDRESS","S", 250, 0);
	ДБФ.Поля.Добавить("SKLAD",	"N",  2, 0);
	ДБФ.Поля.Добавить("COSTYPE",	"N",  2, 0);

	ДБФ.СоздатьФайл(имяФайла);

	ДБФ.Кодировка = КодировкаXBase.OEM;
	//ДБФ.Кодировка = Кодировка;
	Возврат ДБФ;	
КонецФункции

Функция СоздатьБазуКонтактов()
	имяФайла = ПапкаОбмена + "\TC.DBF";
	ДБФ = Новый XBase();

	ДБФ.Поля.Добавить("ID",   "S",  55, 0);
	ДБФ.Поля.Добавить("FIO",  "S", 100, 0);
	ДБФ.Поля.Добавить("PHONE","S",  50, 0);

	ДБФ.СоздатьФайл(имяФайла);

	ДБФ.Кодировка = КодировкаXBase.OEM;
	Возврат ДБФ;
КонецФункции

Функция СоздатьБазуДоговоров()
	имяФайла = ПапкаОбмена + "\TDG.DBF";
	ДБФ = Новый XBase();

	ДБФ.Поля.Добавить("ID",   "S",  55, 0);
	ДБФ.Поля.Добавить("ID_D",  "S", 55, 0);
	ДБФ.Поля.Добавить("NAME","S",  50, 0);

	ДБФ.СоздатьФайл(имяФайла);

	ДБФ.Кодировка = КодировкаXBase.OEM;
	Возврат ДБФ;
КонецФункции


Процедура ЗакрытьБазуПартнеров(партнеры, контакты, договоры, текАгент)
	Если текАгент = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	партнеры.ЗакрытьФайл();	
	контакты.ЗакрытьФайл();
	договоры.ЗакрытьФайл();
	
	кодАгента = КодОбъекта(текАгент);
	
	ПереместитьФайл(ПапкаОбмена + "\TC.DBF", ПапкаОбмена + "\C" + кодАгента + ".DBF");
	ПереместитьФайл(ПапкаОбмена + "\TO.DBF", ПапкаОбмена + "\O" + кодАгента + ".DBF");
	ПереместитьФайл(ПапкаОбмена + "\TDG.DBF", ПапкаОбмена + "\DG" + кодАгента + ".DBF");
КонецПроцедуры

Функция ВыгрузитьКонтакты(Партнер)
	СписокКонтактов = Новый СписокЗначений;
	Для каждого стр из Партнер.КонтактнаяИнформация Цикл
		Если стр.Тип = Перечисления.ТипыКонтактнойИнформации.Телефон Тогда
			тел = ?(СтрДлина(стр.НомерТелефона) = 0, стр.Представление, стр.НомерТелефона);
			Если Не ПустаяСтрока(тел) Тогда
				СписокКонтактов.Добавить(тел, партнер.Наименование);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Возврат СписокКонтактов;
КонецФункции

Функция АдресКонтрагента(Контрагент)
	Возврат УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Контрагент, Справочники.ВидыКонтактнойИнформации.АдресПартнера);
КонецФункции

Процедура ЗаписатьКонтакты(Контакты, СписокКонтактов, код)
	Если СписокКонтактов = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого стр Из СписокКонтактов Цикл
		Контакты.Добавить();
		
		Контакты.ID = код;
		Контакты.FIO = стр.Представление;
		Контакты.PHONE = стр.Значение;
		
		Контакты.Записать();
	КонецЦикла;
КонецПроцедуры

Процедура ВыгрузкаКонтрагентов(ТипыЦен, Склады)
	агенты = ЗагрузитьАгентов();
	текАгент = Неопределено;
	
	контакты = Неопределено;
	партнеры = Неопределено;
	договоры = Неопределено;
	
	кагенты = СоздатьБазуПартнеров("TORGS.DBF");
	
	ЗапросДог = Новый Запрос;
	ЗапросДог.Текст = 
		"ВЫБРАТЬ
		|	ДоговорыКонтрагентов.Партнер КАК Партнер,
		|	ДоговорыКонтрагентов.Ссылка КАК Ссылка,
		|	ДоговорыКонтрагентов.Партнер.Родитель КАК ПартнерРодитель
		|ИЗ
		|	Справочник.ДоговорыКонтрагентов КАК ДоговорыКонтрагентов
		|ГДЕ
		|	ДоговорыКонтрагентов.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыДоговоровКонтрагентов.Действует)
		|	И (ДоговорыКонтрагентов.Партнер = &Партнер
		|			ИЛИ ДоговорыКонтрагентов.Партнер = &ГоловнойПартнер)";	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	УсловияОбслуживанияПартнеровТорговымиПредставителями.Соглашение КАК Соглашение,
		|	УсловияОбслуживанияПартнеровТорговымиПредставителями.ТорговыйПредставитель КАК Агент,
		|	УсловияОбслуживанияПартнеровТорговымиПредставителями.Соглашение.Партнер КАК Партнер,
		|	УсловияОбслуживанияПартнеровТорговымиПредставителями.Соглашение.ВидЦен КАК ВидЦены,
		|	УсловияОбслуживанияПартнеровТорговымиПредставителями.Соглашение.Склад КАК Склад
		|ИЗ
		|	Справочник.УсловияОбслуживанияПартнеровТорговымиПредставителями КАК УсловияОбслуживанияПартнеровТорговымиПредставителями
		|
		|УПОРЯДОЧИТЬ ПО
		|	УсловияОбслуживанияПартнеровТорговымиПредставителями.ТорговыйПредставитель";
	выб = Запрос.Выполнить().Выбрать();
	Пока выб.Следующий() Цикл
		Если Не ЗначениеЗаполнено(выб.Партнер) Тогда
			Продолжить;
		КонецЕсли;
		
		Если текАгент <> выб.Агент Тогда
			ЗакрытьБазуПартнеров(партнеры, контакты, договоры, текАгент);

			текАгент = выб.Агент;
			ДобавитьАгента(Агенты, текАгент);
			партнеры = СоздатьБазуПартнеров("TO.DBF");
			Контакты = СоздатьБазуКонтактов();
			договоры = СоздатьБазуДоговоров();
		КонецЕсли;
		
		код = КодОбъекта(выб.Соглашение);
		видЦены = ?(выб.ВидЦены = Null или выб.ВидЦены.Пустая(), ТипЦеныПрайс, выб.ВидЦены);
		
		инд = ТипыЦен.Найти(видЦены);
		Если инд = Неопределено Тогда 
			инд = типыЦен.Количество();
			типыЦен.Добавить(видЦены);
		КонецЕсли;
		
		скинд = 0;
		Если ЗначениеЗаполнено(выб.Склад) Тогда
			скинд = склады.Найти(выб.Склад);
			Если скинд = Неопределено Тогда
				скинд = склады.Количество();
				склады.Добавить(выб.Склад);
			КонецЕсли;
		КонецЕсли;
		
		партнеры.Добавить();
		партнеры.ID = код;
		партнеры.NAME = выб.Партнер.Наименование;
		партнеры.ADDRESS = АдресКонтрагента(выб.Партнер);
		партнеры.SKLAD = скинд;
		партнеры.COSTYPE = инд;
		партнеры.Записать();
		
		кагенты.Добавить();
		кагенты.ID = код;
		кагенты.NAME = выб.Партнер.Наименование;
		кагенты.ADDRESS = АдресКонтрагента(выб.Партнер);
		кагенты.SKLAD = скинд;
		кагенты.COSTYPE = инд;
		кагенты.Записать();
		
		ЗапросДог.УстановитьПараметр("Партнер", выб.Партнер);
		ЗапросДог.УстановитьПараметр("ГоловнойПартнер", выб.Партнер.Родитель);
		вд = ЗапросДог.Выполнить().Выбрать();
		Пока вд.Следующий() Цикл
			договоры.Добавить();
			договоры.ID = код;
			договоры.ID_D = КодОбъекта(вд.Ссылка);
			договоры.NAME = вд.Ссылка.Наименование;
			договоры.Записать();
		КонецЦикла;
		
		списокКонтактов = ВыгрузитьКонтакты(выб.Партнер);
		ЗаписатьКонтакты(контакты, списокКонтактов, код);
	КонецЦикла;
	
	кагенты.ЗакрытьФайл();
	ПереместитьФайл(ПапкаОбмена + "\TORGS.DBF", ПапкаОбмена + "\ORGS.DBF");
	
	ЗакрытьБазуПартнеров(партнеры, контакты, договоры, текАгент);
	ЗаписатьАгентов(агенты);
КонецПроцедуры
                                                 		
Процедура ВыгрузитьПапкиТоваров()
	КодировкаБазы = КодировкаXBase.OEM; 
	
	имяФайла =  ПапкаОбмена + "\TFLD.dbf";
	файл = Новый Файл(имяФайла);
	Если файл.Существует() Тогда
		УдалитьФайлы(имяФайла, "");
	КонецЕсли;

	ДБФ = Новый XBase();

	ДБФ.Поля.Добавить("ID",   "S", 15, 0);
	ДБФ.Поля.Добавить("LEVEL","N",  6, 0);
	ДБФ.Поля.Добавить("NAME", "S", 50, 0);

	ДБФ.СоздатьФайл(имяФайла);
	ДБФ.Кодировка = КодировкаБазы;
	папки = ДБФ;
	
	папкиТоваров = Справочники.Номенклатура.ВыбратьИерархически();
	Пока папкиТоваров.Следующий() Цикл
		Если папкиТоваров.ПометкаУдаления или (папкиТоваров.ЭтоГруппа = Ложь) Тогда
			Продолжить;
		КонецЕсли;
		
		папки.Добавить();
		папки.ID = КодОбъекта(папкиТоваров);
		папки.NAME = папкиТоваров.Наименование;
		папки.LEVEL = папкиТоваров.УровеньВВыборке();
		папки.Записать();
	КонецЦикла;
	
	//ЗакрытьБазуПапокТоваров(папки);
	папки.ЗакрытьФайл();
	
	ПереместитьФайл(имяФайла, ПапкаОбмена + "\FOLDERS.DBF");
КонецПроцедуры

Функция СоздатьБазуТоваров(ПоляЦен, ПоляСкладов)
	имяФайла = ПапкаОбмена + "\TWH.DBF";
	файл = Новый Файл(имяФайла);
	
	ДБФ = Новый XBase();

	ДБФ.Поля.Добавить("ID",    "S", 15, 0);
	ДБФ.Поля.Добавить("FOLDER","S", 15, 0);
	ДБФ.Поля.Добавить("NAME",  "S",100, 0);
	ДБФ.Поля.Добавить("INPACK","N",  8, 3);
	ДБФ.Поля.Добавить("QTY",   "N", 10, 3);
	ДБФ.Поля.Добавить("WEIGHT","N", 10, 3);
	
	// добавим поля для цен
	Для сч = 1 По ПоляЦен.Количество() Цикл
		ДБФ.Поля.Добавить("COST" + Строка(сч), "N", 10, 2);
	КонецЦикла;
	
	Для сч = 2 По ПоляСкладов.Количество() Цикл
		ДБФ.Поля.Добавить("QTY" + Строка(сч), "N", 10, 3);
	КонецЦикла;

	ДБФ.СоздатьФайл(имяФайла);

	ДБФ.Кодировка = КодировкаXBase.OEM;
	Возврат ДБФ;
КонецФункции

Функция ВыгрузитьОстатки(Склады, вн)
	
	рет = Новый ТаблицаЗначений;
	рет.Колонки.Добавить("Товар");
	рет.Колонки.Добавить("Остатки");
	
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ТоварыНаСкладахОстатки.Номенклатура КАК Товар,
		|	ТоварыНаСкладахОстатки.Склад КАК Склад,
		|	ТоварыНаСкладахОстатки.ВНаличииОстаток КАК Количество
		|ИЗ
		|	РегистрНакопления.ТоварыНаСкладах.Остатки(
		|			,
		|			Склад В (&Склады)
		|				И Номенклатура.ВидНоменклатуры В (&ВидНоменклатуры)) КАК ТоварыНаСкладахОстатки
		|
		|УПОРЯДОЧИТЬ ПО
		|	Товар";

		//"ВЫБРАТЬ
		//|	ТоварыОрганизацийОстатки.КоличествоОстаток КАК Количество,
		//|	ТоварыОрганизацийОстатки.Организация КАК Организация,
		//|	ТоварыОрганизацийОстатки.АналитикаУчетаНоменклатуры.Ссылка КАК Товар
		//|ИЗ
		//|	РегистрНакопления.ТоварыОрганизаций.Остатки(
		//|			,
		//|			Организация В (&Организации)
		//|				И АналитикаУчетаНоменклатуры.Номенклатура.ВидНоменклатуры В (&ВидНоменклатуры)) КАК ТоварыОрганизацийОстатки
		//|
		//|УПОРЯДОЧИТЬ ПО
		//|	Товар";
	Запрос.УстановитьПараметр("Склады", Склады);
	Запрос.УстановитьПараметр("ВидНоменклатуры", вн);
	
	выб = Запрос.Выполнить().Выбрать();
	ТекТовар = Неопределено;
	Пока выб.Следующий() Цикл
		Если текТовар <> выб.Товар Тогда
			стр = рет.Добавить();
			стр.Товар = СокрЛП(выб.Товар.Код);
			стр.Остатки = Новый Соответствие;
			
			текТовар = выб.Товар;
		КонецЕсли;
		
		инд = Склады.Найти(выб.Склад);
		колонка = ?(инд = 0, "QTY", "QTY" + Строка(инд+1));
		стр.Остатки.Вставить(колонка, выб.Количество);
	КонецЦикла;
	
	Возврат рет;	
КонецФункции

Процедура ВыгрузитьТовары(ВидыЦен, Склады)
	вн = ВидыНоменклатуры.ВыгрузитьКолонку("ВидНоменклатуры");
	товары = СоздатьБазуТоваров(ВидыЦен, Склады);
	
	остатки = ВыгрузитьОстатки(Склады, вн);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ЦеныНоменклатурыСрезПоследних.Номенклатура КАК Номенклатура,
		|	ЦеныНоменклатурыСрезПоследних.Цена КАК Цена,
		|	ЦеныНоменклатурыСрезПоследних.Упаковка КАК Упаковка,
		|	ЦеныНоменклатурыСрезПоследних.ВидЦены КАК ВидЦены
		|ИЗ
		|	РегистрСведений.ЦеныНоменклатуры25.СрезПоследних КАК ЦеныНоменклатурыСрезПоследних
		|ГДЕ
		|	ЦеныНоменклатурыСрезПоследних.Номенклатура.ВидНоменклатуры В(&ВидНоменклатуры)
		|	И ЦеныНоменклатурыСрезПоследних.ВидЦены В(&ВидЦены)
		|
		|УПОРЯДОЧИТЬ ПО
		|	Номенклатура";
	
	Запрос.УстановитьПараметр("ВидНоменклатуры", вн);
	Запрос.УстановитьПараметр("ВидЦены", видыЦен);
	
	текТовар = Неопределено;
	
	выб = Запрос.Выполнить().Выбрать();	
	Пока выб.Следующий() Цикл
		Если текТовар <> выб.Номенклатура Тогда
			Если текТовар <> Неопределено Тогда
				товары.Записать();
			КонецЕсли;
			
			текТовар = выб.Номенклатура;
			товары.Добавить();
			
			товары.ID = КодОбъекта(текТовар);
			фильтр = Новый Структура("Товар", СокрЛП(текТовар.Код));
			строки = остатки.НайтиСтроки(фильтр);
			Для каждого строка из строки Цикл
				Для Каждого эл из строка.Остатки Цикл
					товары[эл.Ключ] = эл.Значение;
				КонецЦикла;
				Прервать;
			КонецЦикла;

			Товары.FOLDER = КодОбъекта(текТовар.Родитель);
			Если ПустаяСтрока(текТовар.Артикул) Тогда
				Товары.NAME = текТовар.Наименование;
			Иначе
				Товары.NAME = текТовар.Артикул + " " + текТовар.Наименование;
			КонецЕсли;
			Если текТовар.ВесИспользовать Тогда
				Товары.WEIGHT = текТовар.ВесЧислитель / текТовар.ВесЗнаменатель;
			КонецЕсли;
			Товары.INPACK = 1;
		КонецЕсли;
			
		инд = ВидыЦен.Найти(выб.ВидЦены) + 1;
		Товары.УстановитьЗначениеПоля("COST" + Строка(инд), выб.Цена);
	КонецЦикла;
	
	Если текТовар <> Неопределено Тогда
		товары.Записать();
	КонецЕсли;
	товары.ЗакрытьФайл();
	ПереместитьФайл(ПапкаОбмена + "\TWH.DBF", ПапкаОбмена + "\WAREHOUS.DBF");
	
КонецПроцедуры

Процедура ВыгрузитьНастройки(цены, склады)
	
	Настройки = Новый ТекстовыйДокумент;
	
	// выгружаем виды цен
	строка = "ВидЦены=";
	Для каждого стр из Цены Цикл
		строка = строка + стр.Наименование + ";";
	КонецЦикла;
	Настройки.ДобавитьСтроку(Лев(строка, СтрДлина(строка) - 1));
	
	// выгружаем фирмы
	строка = "Склады=";
	Для каждого стр из склады Цикл
		строка = строка + стр.Наименование + ";";
	КонецЦикла;
	Настройки.ДобавитьСтроку(Лев(строка, СтрДлина(строка) - 1));
	
	строка = "МожноИзменятьЦену=0";
	Настройки.ДобавитьСтроку(строка);
	
	строка = "ДатаДоставки=1";
	Настройки.ДобавитьСтроку(строка);
	
	ИмяФайла = ПапкаОбмена + "\order.cfg";
	Настройки.Записать(ИмяФайла, КодировкаТекста.ANSI);
КонецПроцедуры

Процедура ВыгрузкаОснДанных() Экспорт
	ВидыЦен = Новый Массив;
	Если не ТипЦеныПрайс.Пустая() Тогда
		ВидыЦен.Добавить(ТипЦеныПрайс);
	КонецЕсли;
	
	склады = Новый Массив;
	ВыгрузкаКонтрагентов(ВидыЦен, склады);
	ВыгрузитьПапкиТоваров();
	ВыгрузитьТовары(ВидыЦен, склады);
	ВыгрузитьНастройки(ВидыЦен, склады);
КонецПроцедуры

Процедура СохранитьПринятыеДокументы(КодАгента, списокЗаявок, ТипДок = Неопределено)
	новФайл = ПапкаОбмена + "\PRD" + КодАгента + ".DBF";
	имяФайла = ПапкаОбмена + "\TPRD.DBF";
	
	ДБФ = Новый XBase();
	ДБФ.Поля.Добавить("CREATED",   "S", 20, 0);
	ДБФ.Поля.Добавить("NUMBER", "S", 20, 0);
	ДБФ.Поля.Добавить("TYPE", "S", 20, 0);
	ДБФ.СоздатьФайл(имяФайла);
	ДБФ.Кодировка = КодировкаXBase.OEM;
	
	ДатаНач = ДобавитьМесяц(ТекущаяДата(), -1);
	нач = Формат(ДатаНач, "ДФ=""ггггММдд000000""");
	
	Если ТипДок = Неопределено Тогда
		ТипДок = "Order";
	КонецЕсли;
	
	датаПров = НачалоМесяца(ДобавитьМесяц(ТекущаяДата(), -1));
	
	файл = Новый Файл(новФайл);
	Если файл.Существует() Тогда
		Ист = Новый XBase();
		врФайл = ПапкаОбмена + "\TPRDSC.DBF";
		КопироватьФайл (новФайл, врФайл);
		Ист.ОткрытьФайл(врФайл);
		Ист.Кодировка = КодировкаXBase.OEM;
		
		Пока Не Ист.ВКонце() Цикл
			Если Дата(СокрЛП(Ист.CREATED)) > датаПров и СокрЛП(Ист.TYPE) <> ТипДок Тогда
				ДБФ.Добавить();
				ЗаполнитьЗначенияСвойств(ДБФ, Ист, "CREATED,NUMBER,TYPE");
				ДБФ.Записать();         
			КонецЕсли;
			Ист.Следующая();
		КонецЦикла;
		Ист.ЗакрытьФайл();
	КонецЕсли;
	
	Для Каждого стр из списокЗаявок Цикл
		Если стр.Значение > нач Тогда
			ДБФ.Добавить();
			ДБФ.CREATED = стр.Значение;
			ДБФ.NUMBER = стр.Представление;
			ДБФ.TYPE = ТипДок;
			ДБФ.Записать();         
		КонецЕсли;
	КонецЦикла;
	
	ДБФ.ЗакрытьФайл();

	ПереместитьФайл(имяФайла, новФайл);
КонецПроцедуры

Функция ЗагрузитьПринятыеДокументы(КодАгента, ПоЗаявкам,ТипДок = Неопределено)
	списокЗаявок = Новый СписокЗначений;	
	Если ТипДок = Неопределено Тогда
		ТипДок = "Order";
	КонецЕсли;
	
	имяФайла = ПапкаОбмена + "\PRD" + КодАгента + ".DBF";
	файл = Новый Файл(имяФайла);
	Если файл.Существует() Тогда
		КопироватьФайл (имяФайла, ПапкаОбмена + "\TPRD.DBF");
		ДБФ = Новый XBase();
		ДБФ.ОткрытьФайл(ПапкаОбмена + "\TPRD.DBF");
		ДБФ.Кодировка = КодировкаXBase.OEM;
		
		ДБФ.Первая();
		Пока Не ДБФ.ВКонце() Цикл
			Если СокрЛП(ДБФ.TYPE) = ТипДок Тогда
				Если ПоЗаявкам Тогда
					списокЗаявок.Добавить(СокрЛП(ДБФ.CREATED), СокрЛП(ДБФ.NUMBER));
				Иначе
					списокЗаявок.Добавить(СокрЛП(ДБФ.NUMBER), СокрЛП(ДБФ.CREATED));
				КонецЕсли;
			КонецЕсли;
			ДБФ.Следующая();
		КонецЦикла;
		
		ДБФ.ЗакрытьФайл();
	КонецЕсли;
	
	Возврат списокЗаявок;
КонецФункции

Процедура Ожидание(Таймаут)
    ВремяЗавершения = CurrentDate() + Таймаут;
    While CurrentDate() < ВремяЗавершения Do
        Try
            Соединение = New FTPConnection("127.0.0.1", 2987);
        Except
        EndTry;                    
    EndDo;
КонецПроцедуры

Функция ЗаписатьДокументСервер(док)
	рет = Ложь;
	
	сч = 0;
	
	ош = "";
	Пока сч < 3 и рет = Ложь Цикл
		Попытка		
			док.Записать();
			рет = Истина;
			Прервать;
		Исключение
			док.УстановитьНовыйНомер();
			Если ош = "" Тогда
				ош = ОписаниеОшибки();
			КонецЕсли;
		КонецПопытки;
		
		Ожидание(4);		
		сч = сч + 1;
	КонецЦикла;
	
	Если Не рет Тогда
		Сообщить(ош);
	КонецЕсли;
	Возврат рет;
КонецФункции

Функция ЗаписатьДокумент(Документ, ВидЦеныДокумента)
	Документ.СуммаДокумента = Документ.Товары.Итог("Сумма");
	ВзаиморасчетыСервер.ПроверитьЗаполнитьЭтапыГрафикаОплаты(Документ);
	
	Возврат ЗаписатьДокументСервер(Документ);	
КонецФункции

Процедура СообщитьОДокументе(Документ, РезультатДок)
	типДок = Строка(ТипЗнч(Документ));
	
	РезультатДок.Добавить(типДок + " № " + Документ.Номер + 
			" от " + Документ.Дата + 
			" контрагент " + Документ.Контрагент.Наименование);
КонецПроцедуры

Функция ПолучитьСтруктуруПолейТЧ(СтруктураДействий)

	Перем СтруктураПараметровДействия;

	СтруктураПолейТЧ = Новый Структура;
	СтруктураПолейТЧ.Вставить("Номенклатура");

	Если СтруктураДействий.Свойство("ОбработатьШтрихкоды", СтруктураПараметровДействия) Тогда
		СтруктураПолейТЧ.Вставить("Характеристика");
		Если Не СтруктураПараметровДействия.НеИспользоватьУпаковки Тогда
			СтруктураПолейТЧ.Вставить("Упаковка");
		КонецЕсли;
		СтруктураПолейТЧ.Вставить("ХарактеристикиИспользуются");
		СтруктураПолейТЧ.Вставить(СтруктураПараметровДействия.ИмяКолонкиКоличество, 0);
	КонецЕсли;

	Если СтруктураДействий.Свойство("ПроверитьСерийныеНомераПоВладельцу") Тогда
		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("КлючСвязиСерийныхНомеров");
	КонецЕсли;

	Если СтруктураДействий.Свойство("ПроверитьХарактеристикуПоВладельцу") Тогда
		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("Характеристика");
		СтруктураПолейТЧ.Вставить("ХарактеристикиИспользуются");
	КонецЕсли;

	Если СтруктураДействий.Свойство("ПроверитьЗаполнитьУпаковкуПоВладельцу") Тогда
		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("Упаковка");
	КонецЕсли;

	Если СтруктураДействий.Свойство("ПроверитьСтатьюАналитикуРасходов") Тогда
		СтруктураПолейТЧ.Вставить("ТипНоменклатуры");
		СтруктураПолейТЧ.Вставить("СтатьяРасходов");
		СтруктураПолейТЧ.Вставить("АналитикаРасходов");
	КонецЕсли;
	
	Если СтруктураДействий.Свойство("ПересчитатьКоличествоЕдиниц") Тогда
		СтруктураПолейТЧ.Вставить("Упаковка");
		СтруктураПолейТЧ.Вставить("КоличествоУпаковок", 0);
		СтруктураПолейТЧ.Вставить("Количество", 0);
	КонецЕсли;

	Если СтруктураДействий.Свойство("ЗаполнитьЦенуПродажи") Тогда
		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("Характеристика");
		СтруктураПолейТЧ.Вставить("Упаковка");
		СтруктураПолейТЧ.Вставить("ВидЦены");
		СтруктураПолейТЧ.Вставить("Цена", 0);
	КонецЕсли;

	Если СтруктураДействий.Свойство("ЗаполнитьНоменклатуруПоНоменклатуреПоставщика") Тогда
		СтруктураПолейТЧ.Вставить("НоменклатураПоставщика");
		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("Характеристика");
		СтруктураПолейТЧ.Вставить("Упаковка");
		СтруктураПолейТЧ.Вставить("ХарактеристикиИспользуются");
	КонецЕсли;

	Если СтруктураДействий.Свойство("ЗаполнитьНоменклатуруПоставщикаПоНоменклатуре") Тогда
		СтруктураПолейТЧ.Вставить("НоменклатураПоставщика");
		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("Характеристика");
		СтруктураПолейТЧ.Вставить("Упаковка");
	КонецЕсли;
	
	Если СтруктураДействий.Свойство("ПроверитьСопоставленнуюНоменклатуруПоставщика") Тогда
		СтруктураПолейТЧ.Вставить("НомерСтроки");
		СтруктураПолейТЧ.Вставить("НоменклатураПоставщика");
		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("Характеристика");
		СтруктураПолейТЧ.Вставить("Упаковка");
	КонецЕсли;
	
	Если СтруктураДействий.Свойство("ЗаполнитьЦенуЗакупки") Тогда
		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("Характеристика");
		СтруктураПолейТЧ.Вставить("Упаковка");
		СтруктураПолейТЧ.Вставить("УсловиеЦеныПоставщика");
		СтруктураПолейТЧ.Вставить("Цена", 0);
	КонецЕсли;
	
	Если СтруктураДействий.Свойство("ЗаполнитьЦенуВОтчетеКомитента") Тогда
		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("Характеристика");
		СтруктураПолейТЧ.Вставить("Упаковка");
		СтруктураПолейТЧ.Вставить("Цена", 0);
	КонецЕсли;

	Если СтруктураДействий.Свойство("ЗаполнитьСтавкуНДС") Тогда
		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("СтавкаНДС");
	КонецЕсли;

	Если СтруктураДействий.Свойство("ПересчитатьСуммуНДС") Тогда
		СтруктураПолейТЧ.Вставить("Сумма"    , 0);
		СтруктураПолейТЧ.Вставить("СтавкаНДС", 0);
		СтруктураПолейТЧ.Вставить("СуммаНДС" , 0);
	КонецЕсли;
	
	Если СтруктураДействий.Свойство("ПересчитатьСуммуСНДС") Тогда
		СтруктураПолейТЧ.Вставить("Сумма"    , 0);
		СтруктураПолейТЧ.Вставить("СуммаНДС" , 0);
		СтруктураПолейТЧ.Вставить("СуммаСНДС", 0);
	КонецЕсли;

	Если СтруктураДействий.Свойство("ПересчитатьСумму") Тогда
		СтруктураПолейТЧ.Вставить("Сумма"             , 0);
		СтруктураПолейТЧ.Вставить("Цена"              , 0);
		СтруктураПолейТЧ.Вставить(?(ПустаяСтрока(СтруктураДействий.ПересчитатьСумму),"КоличествоУпаковок",СтруктураДействий.ПересчитатьСумму), 0);
	КонецЕсли;

	Если СтруктураДействий.Свойство("ПересчитатьСуммуСУчетомРучнойСкидки") Тогда
		СтруктураПолейТЧ.Вставить("Сумма"              , 0);
		СтруктураПолейТЧ.Вставить("Количество"         , 0);
		СтруктураПолейТЧ.Вставить("Цена      "         , 0);
		СтруктураПолейТЧ.Вставить("СуммаРучнойСкидки"  , 0);
		СтруктураПолейТЧ.Вставить("ПроцентРучнойСкидки", 0);
	КонецЕсли;
	
	Если СтруктураДействий.Свойство("ПересчитатьСуммуСУчетомАвтоматическойСкидки") Тогда
		СтруктураПолейТЧ.Вставить("Сумма"                      , 0);
		СтруктураПолейТЧ.Вставить("СуммаАвтоматическойСкидки"  , 0);
		СтруктураПолейТЧ.Вставить("ПроцентАвтоматическойСкидки", 0);
	КонецЕсли;

	Если СтруктураДействий.Свойство("ПересчитатьЦенуЗаУпаковку") Тогда
		СтруктураПолейТЧ.Вставить("Цена"      , 0);
		СтруктураПолейТЧ.Вставить("Количество", 0);
	КонецЕсли;

	Если СтруктураДействий.Свойство("ПересчитатьЦенуСкидкуПоСуммеВПродажах") Тогда
		СтруктураПолейТЧ.Вставить("Цена"                       , 0);
		СтруктураПолейТЧ.Вставить("Сумма"                      , 0);
		СтруктураПолейТЧ.Вставить("СуммаАвтоматическойСкидки"  , 0);
		СтруктураПолейТЧ.Вставить("ПроцентАвтоматическойСкидки", 0);
		СтруктураПолейТЧ.Вставить("СуммаРучнойСкидки"          , 0);
		СтруктураПолейТЧ.Вставить("ПроцентРучнойСкидки"        , 0);
		СтруктураПолейТЧ.Вставить("КоличествоУпаковок "        , 0);
	КонецЕсли;
	
	Если СтруктураДействий.Свойство("ПересчитатьЦенуСкидкуПоСуммеВЗакупках") Тогда
		СтруктураПолейТЧ.Вставить("Цена"                       , 0);
		СтруктураПолейТЧ.Вставить("Сумма"                      , 0);
		СтруктураПолейТЧ.Вставить("СуммаРучнойСкидки"          , 0);
		СтруктураПолейТЧ.Вставить("ПроцентРучнойСкидки"        , 0);
		СтруктураПолейТЧ.Вставить("КоличествоУпаковок "        , 0);
	КонецЕсли;
	
	Если СтруктураДействий.Свойство("ПересчитатьКоличествоУпаковок") Тогда
		СтруктураПолейТЧ.Вставить("Упаковка");
		СтруктураПолейТЧ.Вставить("КоличествоУпаковокПоДаннымУчета", 0);
		СтруктураПолейТЧ.Вставить("КоличествоПоДаннымУчета"        , 0);
	КонецЕсли;

	Если СтруктураДействий.Свойство("ПересчитатьКоличествоЕдиницПлан") Тогда
		СтруктураПолейТЧ.Вставить("Упаковка");
		СтруктураПолейТЧ.Вставить("КоличествоУпаковокПлан", 0);
		СтруктураПолейТЧ.Вставить("КоличествоПлан"        , 0);
	КонецЕсли;
	
	Если СтруктураДействий.Свойство("ПроверитьЗаполнитьСклад") Тогда
		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("Склад");
		СтруктураПолейТЧ.Вставить("ТипНоменклатуры");
	КонецЕсли;
	
	ИмяФакта = Неопределено;
	
	Если СтруктураДействий.Свойство("ПересчитатьКоличествоЕдиницФакт",ИмяФакта) Тогда
		СтруктураПолейТЧ.Вставить("Упаковка");
		СтруктураПолейТЧ.Вставить("КоличествоУпаковок"+ИмяФакта, 0);
		СтруктураПолейТЧ.Вставить("Количество"+ИмяФакта    , 0);
	КонецЕсли;

	Если СтруктураДействий.Свойство("ЗаполнитьУсловияПродаж") Тогда
		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("Характеристика");
		СтруктураПолейТЧ.Вставить("Упаковка");
		СтруктураПолейТЧ.Вставить("Цена", 0);
		СтруктураПолейТЧ.Вставить("ВидЦены");
		СтруктураПолейТЧ.Вставить("СтавкаНДС");
	КонецЕсли;

	Если СтруктураДействий.Свойство("ЗаполнитьЦенуПродажиРозница") Тогда
		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("Характеристика");
		СтруктураПолейТЧ.Вставить("Упаковка");
		СтруктураПолейТЧ.Вставить("ВидЦены");
		СтруктураПолейТЧ.Вставить("Цена", 0);
	КонецЕсли;
	
	Если СтруктураДействий.Свойство("ЗаполнитьРеквизитыПоНоменклатуре") Тогда
		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("ВестиУчетПоГТД");
		СтруктураПолейТЧ.Вставить("ЭтоУслуга");
	КонецЕсли;
	
	Если СтруктураДействий.Свойство("ЗаполнитьТипНоменклатуры") Тогда
		СтруктураПолейТЧ.Вставить("ТипНоменклатуры");
		СтруктураПолейТЧ.Вставить("Номенклатура");
	КонецЕсли;
	
	Если СтруктураДействий.Свойство("УстановитьАктивность") Тогда
		СтруктураПолейТЧ.Вставить("Активность");
		СтруктураПолейТЧ.Вставить("ИндексКартинки");
	КонецЕсли;
	
	Если СтруктураДействий.Свойство("ЗаполнитьХарактеристикуНекачественногоТовара") 
		ИЛИ СтруктураДействий.Свойство("ЗаполнитьУпаковкуНекачественногоТовара") Тогда
		СтруктураПолейТЧ.Вставить("НоменклатураИсходногоКачества");
		СтруктураПолейТЧ.Вставить("ХарактеристикиИспользуются");
	КонецЕсли;

	Если СтруктураДействий.Свойство("ПроверитьЗаполнитьВариантКомплектацииПоВладельцу") Тогда

		СтруктураПолейТЧ.Вставить("Номенклатура");
		СтруктураПолейТЧ.Вставить("Характеристика");
		СтруктураПолейТЧ.Вставить("ВариантКомплектации");
		СтруктураПолейТЧ.Вставить("ХарактеристикиИспользуются");

	КонецЕсли;
	
	Если СтруктураДействий.Свойство("ЗаполнитьФлагРасхождение") Тогда

		СтруктураПолейТЧ.Вставить("Расхождение");
		СтруктураПолейТЧ.Вставить("КоличествоУпаковок");
		СтруктураПолейТЧ.Вставить("КоличествоУпаковокФакт");
		
	КонецЕсли;

	Если СтруктураДействий.Свойство("ЗаполнитьФлагИзлишекПорча") Тогда

		СтруктураПолейТЧ.Вставить("ИзлишекПорча");
		
	КонецЕсли;

	Возврат СтруктураПолейТЧ;

КонецФункции

Функция ПолучитьДанныеТекущейСтроки(ТекущаяСтрока, СтруктураПолейТЧ)

	ДанныеТекущейСтроки = Новый Структура;
	Для Каждого КлючИЗначение Из СтруктураПолейТЧ Цикл
		ДанныеТекущейСтроки.Вставить(КлючИЗначение.Ключ, КлючИЗначение.Значение);
	КонецЦикла;
	ЗаполнитьЗначенияСвойств(ДанныеТекущейСтроки, ТекущаяСтрока);

	Возврат ДанныеТекущейСтроки;

КонецФункции

Процедура ДобавитьТовар(Документ, ДокИст)
	Товар = Документ.Товары.Добавить();
	
	Номенклатура = ПолучитьОбъект(Справочники.Номенклатура, ДокИст.ID_I);
	Товар.Номенклатура = Номенклатура;
	
	Если ЗначениеЗаполнено(Документ.Соглашение.ВидЦен) Тогда
		Товар.ВидЦены = Документ.Соглашение.ВидЦен;
	Иначе
		Товар.ВидЦены = ТипЦеныПрайс;
	КонецЕсли;
	
	Товар.КоличествоУпаковок = ДокИст.QTY;
	
	Данные = Новый Структура;	
	мета = Документ.Метаданные();
	Для Каждого рек из мета.ТабличныеЧасти.Товары.Реквизиты Цикл
		Данные.Вставить(рек.Имя);
	КонецЦикла;
	ЗаполнитьЗначенияСвойств(Данные, Товар);
	Данные.Вставить("ТипНоменклатуры");
	Данные.Вставить("ХарактеристикиИспользуются");
	
	СтруктураПересчетаСуммы = ОбработкаТабличнойЧастиКлиентСервер.ПараметрыПересчетаСуммыНДСВСтрокеТЧ(Документ);
	
	СтруктураДействий = Новый Структура;
	
	Если Товар.Упаковка.Пустая() Тогда
		СтруктураДействий.Вставить("ПроверитьЗаполнитьУпаковкуПоВладельцу", Товар.Упаковка);
	КонецЕсли;
	СтруктураДействий.Вставить("ПересчитатьКоличествоЕдиниц");
	СтруктураДействий.Вставить("ПроверитьЗаполнитьСклад", ОбработкаТабличнойЧастиКлиентСервер.ПолучитьСтруктуруЗаполненияСкладаВСтрокеТЧ(Документ, Ложь));
	
	СтруктураДействий.Вставить("ЗаполнитьУсловияПродаж"
		, ОбработкаТабличнойЧастиКлиентСервер.ПолучитьСтруктуруЗаполненияУсловийПродажВСтрокеТЧ(Документ));
	СтруктураДействий.Вставить("ЗаполнитьСтавкуНДС", Новый Структура("НалогообложениеНДС, Организация, Дата, ПоДатеОтгрузки"
		, Документ.НалогообложениеНДС
		, Документ.Организация
		, Документ.Дата
		, Истина));
	СтруктураДействий.Вставить("ЗаполнитьСтавкуНДСВозвратнойТары", Документ.ВернутьМногооборотнуюТару);
	СтруктураДействий.Вставить("ПересчитатьСуммуНДС", СтруктураПересчетаСуммы);
	СтруктураДействий.Вставить("ПересчитатьСуммуСНДС", СтруктураПересчетаСуммы);
	СтруктураДействий.Вставить("ПересчитатьСумму");
	СтруктураДействий.Вставить("ПересчитатьСуммуСУчетомРучнойСкидки", Новый Структура("Очищать", Истина));
	СтруктураДействий.Вставить("ПересчитатьСуммуСУчетомАвтоматическойСкидки", Новый Структура("Очищать", Истина));
	СтруктураДействий.Вставить("ЗаполнитьПризнакТипНоменклатуры", Новый Структура("Номенклатура", "ТипНоменклатуры"));
	
	ОбработкаТабличнойЧастиСервер.ОбработатьСтрокуТЧ(Данные, СтруктураДействий, Неопределено);
	ЗаполнитьЗначенияСвойств(Товар, Данные);
	//ОбработкаТабличнойЧастиСервер.ОбработатьСтрокуТЧ(Товар, СтруктураДействий, Неопределено);
КонецПроцедуры

Функция СоздатьЗаказ(ДокИст, Агент)
	соглашение = ПолучитьОбъект(Справочники.СоглашенияСКлиентами, ДокИст.ID);

	Документ = Документы.ЗаказКлиента.СоздатьДокумент();
	Документ.Дата = ТекущаяДата();
	Документ.ДатаОтгрузки = ДокИст.DATE;
	Документ.НеОтгружатьЧастями = Истина;
	
	Документ.Менеджер = Агент;
	Документ.Автор = Агент;
	Документ.Подразделение = Документ.Менеджер.Подразделение;
	
	Документ.Соглашение = соглашение;
	Документ.ЗаполнитьУсловияПродажПоСоглашению();	

	Документ.Организация = соглашение.Организация;
	Документ.Контрагент = соглашение.Контрагент;
	Документ.Партнер = соглашение.Партнер;
	Документ.Склад = соглашение.Склад;
	Документ.ХозяйственнаяОперация = Перечисления.ХозяйственныеОперации.РеализацияКлиенту;
	Документ.СпособДоставки = Перечисления.СпособыДоставки.ДоКлиента;
	Документ.Статус = Перечисления.СтатусыЗаказовКлиентов.КОтгрузке;
	Документ.Приоритет = Справочники.Приоритеты.ПолучитьПриоритетПоУмолчанию(Неопределено);
	
	Если ДокИст.Поля.Найти("dogovor") <> Неопределено Тогда
		договор = ПолучитьОбъект(Справочники.ДоговорыКонтрагентов, ДокИст.dogovor);
		Если не договор.Пустая() Тогда
			Документ.Договор = договор.Ссылка;
			Документ.Контрагент = договор.Контрагент;
			Документ.Организация = договор.Организация;
		КонецЕсли;
	Иначе
		двыб = Справочники.ДоговорыКонтрагентов.Выбрать(,Документ.Контрагент);
		Если двыб.Следующий() Тогда
			Документ.Договор = двыб.Ссылка;
		КонецЕсли;
	КонецЕсли;
	
	Документ.Комментарий = ДокИст.REMARK;
	Документ.АдресДоставки = ФормированиеПечатныхФорм.ПолучитьАдресИзКонтактнойИнформации(Документ.Партнер);

	Возврат Документ;
КонецФункции

Функция ЗагрузкаЗаявокАгента(временныйФайл, КодАгента, ПринятыеЗаказы, РезультатДок)
	Агент = ПолучитьОбъект(Справочники.Пользователи, КодАгента);
	Документ = Неопределено;
	ВидЦеныДокумента = Неопределено;
	
	СчДок = 0;
	
	Заказ = Новый XBase(временныйФайл);
	
	Заказ.Кодировка = КодировкаXBase.OEM;
	КодЗаказа = "";
	
	Заказ.Первая();		
	Пока Заказ.ВКонце() = 0 Цикл			
		//
		// Проверим признак начала заявки
		//
		Если Заказ.ORDFLAG <> 0 Тогда
			//
			// Запишем предыдущий документ
			//
			Если Документ <> Неопределено Тогда
				ЗаписатьДокумент(Документ, ВидЦеныДокумента);
				
				СчДок = СчДок + 1;
				СообщитьОДокументе(Документ, РезультатДок);
				
				ПринятыеЗаказы.Добавить(КодЗаказа, Документ.Номер);
				Документ = Неопределено;	
			КонецЕсли;
			
			КодЗаказа = СокрЛП(Заказ.CREATED);
			значЗаказ = ПринятыеЗаказы.НайтиПоЗначению(КодЗаказа);
			Если значЗаказ <> Неопределено Тогда			
				//Сообщить("Повторный заказ " + значЗаказ);
			Иначе			
				Документ = СоздатьЗаказ(Заказ, Агент);
			КонецЕсли;
		КонецЕсли;
		
		Если Документ <> Неопределено Тогда
			ДобавитьТовар(Документ, Заказ);
		КонецЕсли;
		
		Заказ.Следующая();
	КонецЦикла;
	
	Если Документ <> Неопределено Тогда
		ЗаписатьДокумент(Документ, ВидЦеныДокумента);
		
		СчДок = СчДок + 1;
		СообщитьОДокументе(Документ, РезультатДок);
		
		ПринятыеЗаказы.Добавить(КодЗаказа, Документ.Номер);
	КонецЕсли;
	
	Заказ.ЗакрытьФайл();
	
	Возврат СчДок;
КонецФункции

Процедура СохранитьОбработанныеДокументы(КодАгента, ПринятыеЗаказы, ТипДок = "")
	имяФайла = ПапкаОбмена + "\POD" + КодАгента + ".DBF";
	имяВр = ПапкаОбмена + "\TPOD.DBF";

	ДБФ = Новый XBase();
	файл = Новый Файл(имяФайла);
	
	Если Не файл.Существует() Тогда
		ДБФ.Поля.Добавить("CREATED", "S", 25, 0);	
		ДБФ.Поля.Добавить("TYPE", "S", 25, 0);	
		ДБФ.СоздатьФайл(имяВр);
	Иначе
		ПереместитьФайл(имяФайла, имяВр);
		ДБФ.ОткрытьФайл(имяВр);
	КонецЕсли;
	
	ДБФ.Кодировка = КодировкаXBase.OEM;
	Для Каждого стр из ПринятыеЗаказы Цикл
		ДБФ.Добавить();
		ДБФ.CREATED = стр.Значение;
		ДБФ.TYPE = ТипДок;
		ДБФ.Записать();
	КонецЦикла;
	ДБФ.ЗакрытьФайл();
	
	ПереместитьФайл(имяВр, имяФайла);
КонецПроцедуры

Процедура ПриемДокументов() Экспорт
	РезультатДок = Новый Массив;
	
	ЗаписьЖурналаРегистрации("ПриемДокументов", УровеньЖурналаРегистрации.Информация, Метаданные, ПапкаОбмена, "Чтение заказов");
	Рез = НайтиФайлы(ПапкаОбмена, "ORD*.DBF");
	
	КодировкаБазы = КодировкаXBase.OEM; 

	Для Каждого файл из Рез Цикл
		имяФайла = файл.Имя;
		разделитель = Найти(ВРег(имяФайла), ".DBF");
		КодАгента = Сред(имяФайла, 4, разделитель-4);
		
		временныйФайл = ПапкаОбмена + "\TORD.DBF";
		ПереместитьФайл(файл.ПолноеИмя, временныйФайл);
		
		ПринятыеЗаказы = ЗагрузитьПринятыеДокументы(КодАгента, Истина);
		
		ЗагрузкаЗаявокАгента(временныйФайл, КодАгента, ПринятыеЗаказы, РезультатДок);
		
		СохранитьОбработанныеДокументы(КодАгента, ПринятыеЗаказы);
		СохранитьПринятыеДокументы(КодАгента, ПринятыеЗаказы);
	КонецЦикла;
	
	Для каждого стр из РезультатДок Цикл
		Сообщить(стр);
	КонецЦикла;
КонецПроцедуры

Процедура ЗагрузитьНастройки() Экспорт
	Попытка
		спр = Справочники.ДополнительныеОтчетыИОбработки.НайтиПоРеквизиту("ИмяОбъекта", "ВыгрузкаНаполеон");
		настройки = спр.ХранилищеНастроек.Получить();
//		настройки = ХранилищеОбщихНастроек.Загрузить("NapoleonExchange","NapoleonExchange");
		Если настройки <> Неопределено Тогда
			Для каждого стр из Настройки Цикл
				Если стр.Ключ <> "ВидыНоменклатуры" Тогда
					Попытка
						ЭтотОбъект[стр.Ключ] = стр.Значение;
					Исключение;	
					КонецПопытки
				Иначе
					Для каждого тст из стр.Значение Цикл
						встр = ВидыНоменклатуры.Добавить();
						встр.ВидНоменклатуры = тст;
					КонецЦикла;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	Исключение	
	КонецПопытки;
КонецПроцедуры

Процедура СохранитьНастройки() Экспорт
	
	настройки = Новый Структура;
	Для каждого стр из Метаданные().Реквизиты Цикл
		настройки.Вставить(стр.Имя, ЭтотОбъект[стр.Имя]);		
	КонецЦикла;
	табл = Новый Массив;
	настройки.Вставить("ВидыНоменклатуры", табл);
	Для каждого стр из ВидыНоменклатуры Цикл
		табл.Добавить(стр.ВидНоменклатуры);
	КонецЦикла;	
	зн = Новый ХранилищеЗначения(настройки);
	спр = Справочники.ДополнительныеОтчетыИОбработки.НайтиПоРеквизиту("ИмяОбъекта", "ВыгрузкаНаполеон");
	спр = спр.ПолучитьОбъект();
	спр.ХранилищеНастроек = зн;
	спр.Записать();
	//ХранилищеОбщихНастроек.Сохранить("NapoleonExchange", "NapoleonExchange", настройки);
КонецПроцедуры

Функция СоздатьБазуОтгрузок()
	имяФайла = ПапкаОбмена + "\TDLV.DBF";
	файл = Новый Файл(имяФайла);
	Если файл.Существует() Тогда
		УдалитьФайлы(имяФайла, "");
	КонецЕсли;

	ДБФ = Новый XBase();

	ДБФ.Поля.Добавить("ID",   "S",  55, 0);
	ДБФ.Поля.Добавить("NUM",  "S",  25, 0);
	ДБФ.Поля.Добавить("ID_I", "S",  25, 0);
	ДБФ.Поля.Добавить("QTY",  "N",  10, 3);
	ДБФ.Поля.Добавить("SUM",  "N",  10, 2);
	ДБФ.Поля.Добавить("SUMD", "N",  10, 2);
	ДБФ.Поля.Добавить("DATE", "D",  8, 0);
	ДБФ.Поля.Добавить("PAYDATE", "D",  8, 0);
	ДБФ.Поля.Добавить("CREATED", "S",  10, 0);

	ДБФ.СоздатьФайл(имяФайла);

	ДБФ.Кодировка = КодировкаXBase.OEM;
	Возврат ДБФ;
КонецФункции

Функция СоздатьБазуДолгов()
	имяФайла = ПапкаОбмена + "\TPAY.DBF";
	файл = Новый Файл(имяФайла);
	Если файл.Существует() Тогда
		УдалитьФайлы(имяФайла, "");
	КонецЕсли;

	ДБФ = Новый XBase();

	ДБФ.Поля.Добавить("ID",   "S",  55, 0);
	ДБФ.Поля.Добавить("DATE", "D",  8, 0);
	ДБФ.Поля.Добавить("NUM",  "S",  25, 0);
	ДБФ.Поля.Добавить("SUM",  "N",  10, 2);

	ДБФ.СоздатьФайл(имяФайла);

	ДБФ.Кодировка = КодировкаXBase.OEM;
	Возврат ДБФ;
КонецФункции

Процедура ЗакрытьБазуДолгов(агент, долги, отгрузки)
	Если агент = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	КодАгента = КодОбъекта(агент);
	
	Если долги <> Неопределено Тогда
		долги.ЗакрытьФайл();
		ПереместитьФайл(ПапкаОбмена + "\TPAY.DBF", ПапкаОбмена + "\PAY" + КодАгента + ".DBF");
	КонецЕсли;
	
	отгрузки.ЗакрытьФайл();	
	ПереместитьФайл(ПапкаОбмена + "\TDLV.DBF", ПапкаОбмена + "\DLV" + КодАгента + ".DBF");
КонецПроцедуры

Функция ЗаписатьОтгрузку(Отгрузки, Реализация, Долг, ПринятыеЗаказы)
	
	долгДок = Реализация.СуммаДокумента;
	Если долгДок > Долг Тогда
		долгДок = Долг;
	КонецЕсли;	
		
	Если Реализация.Контрагент.Наименование <> "ДЕГУСТАЦИЯ" Тогда
		кодЗаказа = "";
		Если ЗначениеЗаполнено(Реализация.ЗаказКлиента) Тогда
			номерЗаказа = ПринятыеЗаказы.НайтиПоЗначению(Реализация.ЗаказКлиента.Номер);
			Если номерЗаказа <> Неопределено Тогда
				кодЗаказа = номерЗаказа.Представление;
			КонецЕсли;
		КонецЕсли;	
		Код = КодОбъекта(Реализация.Соглашение);;
		
		ДатаПлатежа = Неопределено;
		Для Каждого пл Из Реализация.ЭтапыГрафикаОплаты	Цикл
			Если ДатаПлатежа = Неопределено или ДатаПлатежа < пл.ДатаПлатежа Тогда
				ДатаПлатежа = пл.ДатаПлатежа;
			КонецЕсли;
		КонецЦикла;
		Если ДатаПлатежа = Неопределено Тогда
			ДатаПлатежа = Реализация.Дата + Реализация.Договор.СрокОплаты * 24 * 3600;
		КонецЕсли;
		
		Для Каждого товар Из Реализация.Товары Цикл
			Отгрузки.Добавить();
			
			Отгрузки.ID = Код;
			Отгрузки.NUM = Реализация.Номер;
			Отгрузки.SUMD = долгДок;
			Отгрузки.DATE = Реализация.Дата;
			Отгрузки.PAYDATE = ДатаПлатежа;
			Отгрузки.CREATED = кодЗаказа;
			
			Отгрузки.ID_I = КодОбъекта(товар.Номенклатура);
			Отгрузки.QTY = товар.Количество;
			Отгрузки.SUM = товар.Сумма;
			
			Отгрузки.Записать();
		КонецЦикла;
	КонецЕсли;
	
	Возврат Долг - долгДок;
КонецФункции

Процедура ВыгрузкаДолгов() Экспорт
	ДатаПо = ТекущаяДата();
	ДатаС = ДобавитьМесяц(ДатаПо, -ГлубинаВыгрузкиДолгов);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	Долги.Агент КАК Агент,
		|	Долги.Долг КАК Долг,
		|	Долги.Соглашение КАК Соглашение,
		|	Документы.Ссылка КАК Документ,
		|	Документы.Дата КАК Дата
		|ИЗ
		|	(ВЫБРАТЬ
		|		СУММА(ЕСТЬNULL(РасчетыСКлиентамиОстатки.СуммаОстаток, 0)) КАК Долг,
		|		АгентыКонтрагенты.Соглашение КАК Соглашение,
		|		АгентыКонтрагенты.Агент КАК Агент,
		|		АгентыКонтрагенты.Партнер КАК Партнер
		|	ИЗ
		|		(ВЫБРАТЬ
		|			УсловияОбслуживанияПартнеровТорговымиПредставителями.Соглашение.Ссылка КАК Соглашение,
		|			УсловияОбслуживанияПартнеровТорговымиПредставителями.ТорговыйПредставитель.Ссылка КАК Агент,
		|			УсловияОбслуживанияПартнеровТорговымиПредставителями.Соглашение.Партнер КАК Партнер,
		|			УсловияОбслуживанияПартнеровТорговымиПредставителями.Соглашение.Организация КАК Организация
		|		ИЗ
		|			Справочник.УсловияОбслуживанияПартнеровТорговымиПредставителями КАК УсловияОбслуживанияПартнеровТорговымиПредставителями) КАК АгентыКонтрагенты
		|			ЛЕВОЕ СОЕДИНЕНИЕ РегистрНакопления.РасчетыСКлиентами.Остатки(&ДатаПо, ) КАК РасчетыСКлиентамиОстатки
		|			ПО АгентыКонтрагенты.Партнер = РасчетыСКлиентамиОстатки.АналитикаУчетаПоПартнерам.Партнер
		|				И АгентыКонтрагенты.Организация = РасчетыСКлиентамиОстатки.АналитикаУчетаПоПартнерам.Организация
		|	
		|	СГРУППИРОВАТЬ ПО
		|		АгентыКонтрагенты.Соглашение,
		|		АгентыКонтрагенты.Агент,
		|		АгентыКонтрагенты.Партнер) КАК Долги
		|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			РеализацияТоваровУслуг.Ссылка КАК Ссылка,
		|			РеализацияТоваровУслуг.Дата КАК Дата,
		|			РеализацияТоваровУслуг.Партнер КАК Партнер
		|		ИЗ
		|			Документ.РеализацияТоваровУслуг КАК РеализацияТоваровУслуг
		|		ГДЕ
		|			РеализацияТоваровУслуг.Дата МЕЖДУ &ДатаС И &ДатаПо) КАК Документы
		|		ПО (Долги.Партнер = Документы.Партнер)
		|
		|УПОРЯДОЧИТЬ ПО
		|	Агент,
		|	Соглашение,
		|	Дата УБЫВ";
	Запрос.УстановитьПараметр("ДатаПо", ДатаПо);
	Запрос.УстановитьПараметр("ДатаС", ДатаС);
	
	Результат = Запрос.Выполнить();
	Выборка = Результат.Выбрать(ОбходРезультатаЗапроса.Прямой);
	
	текАгент = Неопределено;
	текСогл = Неопределено;
	СуммаДолга = 0;
	
	Долги = Неопределено;
	Отгрузки = Неопределено;
	Долги = Неопределено;
	ПринятыеЗаказы = Неопределено;
	
	Пока Выборка.Следующий() Цикл
		Если Не ЗначениеЗаполнено(выборка.Агент) Тогда
			Продолжить;
		КонецЕсли;
		
		Если текАгент <> Выборка.Агент Тогда
			Если СуммаДолга <> 0 Тогда
				Долги.Добавить();
				Долги.ID = КодОбъекта(текСогл);
				Долги.NUM = "Нач.Сальдо";
				Долги.SUM = СуммаДолга;
				Долги.DATE = ДатаС;
				Долги.Записать();
			КонецЕсли;

			ЗакрытьБазуДолгов(текАгент, Долги, отгрузки);
			текАгент = Выборка.Агент;
			текСогл = Неопределено;
			СуммаДолга = 0;
			
			ПринятыеЗаказы = ЗагрузитьПринятыеДокументы(КодОбъекта(текАгент), Ложь);

			Отгрузки = СоздатьБазуОтгрузок();
			Долги = СоздатьБазуДолгов();			
		КонецЕсли;
		
		Если текСогл <> Выборка.Соглашение Тогда
			Если СуммаДолга <> 0 Тогда
				Долги.Добавить();
				Долги.ID = КодОбъекта(текСогл);
				Долги.SUM = СуммаДолга;
				Долги.NUM = "Нач.Сальдо";
				Долги.DATE = ДатаС;
				Долги.Записать();
			КонецЕсли;
			
			текСогл = Выборка.Соглашение;
			СуммаДолга = Выборка.Долг;
		КонецЕсли;

		Если ЗначениеЗаполнено(Выборка.Документ) Тогда
			СуммаДолга = ЗаписатьОтгрузку(Отгрузки, Выборка.Документ, СуммаДолга, ПринятыеЗаказы);
		КонецЕсли;
	КонецЦикла;
		
	Если СуммаДолга <> 0 Тогда
		Долги.Добавить();
		Долги.ID = КодОбъекта(текСогл);
		Долги.SUM = СуммаДолга;
		Долги.DATE = ДатаС;
		Долги.Записать();
	КонецЕсли;
	ЗакрытьБазуДолгов(текАгент, Долги, отгрузки);	
	
КонецПроцедуры