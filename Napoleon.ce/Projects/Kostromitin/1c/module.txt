// 
// Copyright (c) 2005 - 2008 "Гильдия разработчиков"
//
// Модуль обмена информацией КПК <-> 1C
//
// 

//
// Синхронизация КПК с базой может происходить в любой момент времени.
// Из-за этого для правильной работы синхронизации с КПК, все файлы сначала создаются с 
// временными именами и только по окончанию работы файлы переименовываются.
// 


//
// если файл users.dbf есть в папке, открыть его, иначе создать
//
Функция ФайлМенеджеров()
	имяФайла = ПапкаОбмена + "\Users.dbf";
	врФайл = ПапкаОбмена + "\Users1.dbf";

	ДБФ = Новый XBase();
	файл = Новый Файл(имяФайла);
	
	Если файл.Существует() Тогда
		КопироватьФайл(имяФайла, врФайл);	
		ДБФ.ОткрытьФайл(врФайл);
	Иначе
		ДБФ.Поля.Добавить("ID",   "S", 55, 0);
		ДБФ.Поля.Добавить("NAME", "S", 100, 0);
		ДБФ.Поля.Добавить("LOGIN", "S", 30, 0);
		ДБФ.Поля.Добавить("PASSWORD", "S", 30, 0);
		ДБФ.СоздатьФайл(врФайл);
	КонецЕсли;

	ДБФ.Кодировка = КодировкаXBase.OEM;
	Возврат ДБФ;
КонецФункции

//
// Создаем временный файл, в который будут записываться информация по контрагентам
//
Функция ФайлОрганизаций(Общий = Ложь)
	ДБФ = Новый XBase();
		
	имяФайла = ПапкаОбмена + ?(Общий, "\TORGS.DBF", "\TO.DBF");

	ДБФ.Поля.Добавить("ID",   "S", 55, 0);
	ДБФ.Поля.Добавить("NAME", "S", 55, 0);
	ДБФ.Поля.Добавить("ADDRESS","S", 100, 0);
	ДБФ.Поля.Добавить("COSTYPE","N", 3, 0);
	ДБФ.Поля.Добавить("PRCTYPE","S", 30, 0);
	ДБФ.СоздатьФайл(имяФайла);

	ДБФ.Кодировка = КодировкаXBase.OEM;
	Возврат ДБФ;
КонецФункции    
	
//
// Создаем временный файл, в который будут записываться информация по контактам
//
Функция ФайлКонтактов(Агент)
	ДБФ = Новый XBase();
	
	имяФайлаАгента = ПапкаОбмена + "\TC" + СокрЛП(Агент.Код) + ".DBF";
	имяФайла = ПапкаОбмена + "\TC.DBF";
	файл = Новый Файл(имяФайлаАгента);
	Если файл.Существует() Тогда
		ПереместитьФайл(имяФайлаАгента, имяФайла);
		ДБФ.ОткрытьФайл(имяФайла);
	Иначе
		ДБФ.Поля.Добавить("ID",   "S",  25, 0);
		ДБФ.Поля.Добавить("FIO",  "S", 100, 0);
		ДБФ.Поля.Добавить("PHONE","S", 50, 0);
		ДБФ.СоздатьФайл(имяФайла);
	КонецЕсли;

	ДБФ.Кодировка = КодировкаXBase.OEM;
	Возврат ДБФ;
КонецФункции    

//
// Создать файл для товаров
//
Функция ФайлТоваров(имяФайла, ТипыЦен)
	файл = Новый Файл(имяФайла);
	Если файл.Существует() Тогда
		УдалитьФайлы(имяФайла, "");
	КонецЕсли;

	ДБФ = Новый XBase();

	ДБФ.Поля.Добавить("ID",    "S", 55, 0);
	ДБФ.Поля.Добавить("FOLDER","S", 15, 0);
	ДБФ.Поля.Добавить("NAME",  "S",100, 0);
	
	сч = 1;
	// добавим поля для цен
	Для Каждого стр из ТипыЦен Цикл
		ДБФ.Поля.Добавить("COST" + Строка(сч), "N", 10, 2);
		сч = сч + 1;
	КонецЦикла;
	
	ДБФ.Поля.Добавить("INPACK","N",  8, 3);
	ДБФ.Поля.Добавить("PRT","N",  1, 0);
	ДБФ.Поля.Добавить("QTY",   "N", 10, 3);
	ДБФ.Поля.Добавить("QTY2",   "N", 10, 3);
	ДБФ.Поля.Добавить("ISLUKOIL", "N", 1, 0);
	ДБФ.Поля.Добавить("WEIGHT","N", 10, 3);
	ДБФ.Поля.Добавить("NODSC", "N", 1, 0);

	ДБФ.СоздатьФайл(имяФайла);

	ДБФ.Кодировка = КодировкаXBase.OEM;
	Возврат ДБФ;
КонецФункции

//
// Создать файл папок товара
//
Функция ФайлПапок(имяФайла)
	файл = Новый Файл(имяФайла);
	Если файл.Существует() Тогда
		УдалитьФайлы(имяФайла, "");
	КонецЕсли;

	ДБФ = Новый XBase();

	ДБФ.Поля.Добавить("ID",   "S", 15, 0);
	ДБФ.Поля.Добавить("LEVEL","N",  6, 0);
	ДБФ.Поля.Добавить("NAME", "S", 50, 0);

	ДБФ.СоздатьФайл(имяФайла);

	ДБФ.Кодировка = КодировкаXBase.OEM;
	Возврат ДБФ;
КонецФункции

//
// создать файл оплат - общий для всех
//
Функция ФайлОплат()
	имяФайла = ПапкаОбмена + "\TPAY.DBF";
	файл = Новый Файл(имяФайла);
	Если файл.Существует() Тогда
		УдалитьФайлы(имяФайла);
	КонецЕсли;
	
	ДБФ = Новый XBase();
	
	ДБФ.Поля.Добавить("ID",  "S", 55, 0);
	ДБФ.Поля.Добавить("NUM", "S", 20, 0);
	ДБФ.Поля.Добавить("DATE","D",  8, 0);
	ДБФ.Поля.Добавить("SUM", "N", 10, 2);

	ДБФ.СоздатьФайл(имяФайла);
	
	ДБФ.Кодировка = КодировкаXBase.OEM;
	Возврат ДБФ;
КонецФункции

Функция ФайлОтгрузок(ДневнойФайл)
	имяФайла = ПапкаОбмена + ?(ДневнойФайл, "\TDDLV.DBF", "\TDLV.DBF");

	ДБФ = Новый XBase();

	ДБФ.Поля.Добавить("ID",  "S", 55, 0);
	ДБФ.Поля.Добавить("NUM", "S", 20, 0);
	ДБФ.Поля.Добавить("DATE","D",  8, 0);
	ДБФ.Поля.Добавить("ID_I","S", 15, 0);
	ДБФ.Поля.Добавить("QTY", "N", 10, 3);
	ДБФ.Поля.Добавить("SUM", "N", 10, 2);
	ДБФ.Поля.Добавить("SUMD", "N", 10, 2);
	ДБФ.Поля.Добавить("PAYDATE", "D", 8, 0);
	ДБФ.Поля.Добавить("CREATED",  "S", 20, 0);

	ДБФ.СоздатьФайл(имяФайла);
	
	ДБФ.Кодировка = КодировкаXBase.OEM;
	Возврат ДБФ;
КонецФункции

Функция ФайлФирм()
	имяФайла = ПапкаОбмена + "\TFM.DBF";

	ДБФ = Новый XBase();
	ДБФ.Поля.Добавить("ID",     "S", 55, 0);
	ДБФ.Поля.Добавить("NAME",   "S", 55, 0);
	ДБФ.Поля.Добавить("TYPE",	"N", 1, 0);
	
	ДБФ.СоздатьФайл(имяФайла);
	
	ДБФ.Кодировка = КодировкаXBase.OEM;
	Возврат ДБФ;
КонецФункции

Процедура ЗакрытьФайлФирм(таблица)
	таблица.ЗакрытьФайл();
	ПереместитьФайл(ПапкаОбмена + "\TFM.DBF", ПапкаОбмена + "\FIRMS.DBF" );
КонецПроцедуры

Процедура ЗакрытьОплатыОтгрузки(оплаты, отгрузки, Агент, ДневнойФайл)
	Если Агент = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	кодАгента = КодАгента(Агент);
	
	Если оплаты <> Неопределено Тогда
		оплаты.ЗакрытьФайл();
		ПереместитьФайл(ПапкаОбмена + "\TPAY.DBF", ПапкаОбмена + "\PAY"+ СокрЛП(кодАгента) + ".DBF" );  
	КонецЕсли;
	
	Если отгрузки <> Неопределено Тогда
		отгрузки.ЗакрытьФайл();
		Если ДневнойФайл Тогда
			ПереместитьФайл(ПапкаОбмена + "\TDDLV.DBF", ПапкаОбмена + "\DDV"+ СокрЛП(кодАгента) + ".DBF" );  
		Иначе
			ПереместитьФайл(ПапкаОбмена + "\TDLV.DBF", ПапкаОбмена + "\DLV"+ СокрЛП(кодАгента) + ".DBF" );  
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

//
// Файл обработанных заказов
//
Функция ФайлОбработанныхЗаказов(кодМенеджера)
	имяФайла = ПапкаОбмена + "\POD" + СокрЛП(кодМенеджера) + ".DBF";
	темпФайл = ПапкаОбмена + "\TPOD.DBF";

	ДБФ = Новый XBase();
	файл = Новый Файл(имяФайла);
	
	Если файл.Существует() Тогда
		ПереместитьФайл(имяФайла, темпФайл);	
		ДБФ.ОткрытьФайл(темпФайл);
	Иначе
		ДБФ.Поля.Добавить("CREATED", "S", 25, 0);	
		ДБФ.Поля.Добавить("TYPE", "S", 25, 0);	
		ДБФ.СоздатьФайл(темпФайл);
	КонецЕсли;
	
	ДБФ.Кодировка = КодировкаXBase.OEM;
	Возврат ДБФ;
КонецФункции    

Функция КодАгента(ТекАгент)
	пКод = Строка(ТекАгент.УникальныйИдентификатор());
	Возврат пКод;
КонецФункции

Процедура ЗакрытьФайлыАгента(ОргDBF, КонтDBF, ТекАгент);
	Если ТекАгент = "" Тогда
		Возврат;
	КонецЕсли;
	
	ОргDBF.ЗакрытьФайл();
	КонтDBF.ЗакрытьФайл();
	
	код = КодАгента(ТекАгент);
	
	ПереместитьФайл(ПапкаОбмена + "\TO.DBF", ПапкаОбмена + "\O" + СокрЛП(код) + ".DBF");  
	ПереместитьФайл(ПапкаОбмена + "\TC.DBF", ПапкаОбмена + "\C" + СокрЛП(код) + ".DBF");  
КонецПроцедуры

Процедура ЗакрытьФайлМенеджеров( Менеджеры)
	Менеджеры.ЗакрытьФайл();
	ПереместитьФайл(ПапкаОбмена + "\USERS1.DBF", ПапкаОбмена + "\USERS.DBF");  	
КонецПроцедуры

Процедура ПереименоватьФайлОплат(Папка)
	ПереместитьФайл(Папка + "\TPAY.DBF", Папка + "\PAYMENT.DBF" );  
КонецПроцедуры

//
// Изменить имена файлов с накладными на доступные для синхронизации
//
Процедура ПереименоватьФайлыНакладных(Папка)
	Рез = НайтиФайлы(Папка, "TLV*.DBF"); 
	Для Каждого файл из Рез Цикл
		имяФайла = файл.Имя;
		ПереместитьФайл(Папка + "\" + имяФайла, Папка + "\D" + Прав(имяФайла, СтрДлина(имяФайла)-1));  
	КонецЦикла;
КонецПроцедуры

//
// Ищем в файле USERS.DBF запись о торговом агенте
//
Процедура ПроверитьИнформациюПоМенеджеру(МенеджерDBF, Агент) 
	МенеджерDBF.Первая();
	пКод = КодАгента(Агент);
	Пока МенеджерDBF.ВКонце() = 0 Цикл
		Если СокрЛП(МенеджерDBF.ID) = пКод Тогда
			МенеджерDBF.NAME = Агент.Наименование;
			МенеджерDBF.Записать();
			Возврат;
		КонецЕсли;
		МенеджерDBF.Следующая();
	КонецЦикла;
	
	МенеджерDBF.Добавить();
	МенеджерDBF.ID = пКод;
	МенеджерDBF.NAME = Агент.Наименование;
	
	//
	// В качестве пароля и логина для КПК назначим количество записей + 1. Так для первого пользователя
	// логин и пароль будет 1/1, для второго 2/2 и т.д.
	//
	МенеджерDBF.PASSWORD = МенеджерDBF.КоличествоЗаписей() + 1;
	МенеджерDBF.LOGIN = МенеджерDBF.КоличествоЗаписей() + 1;
	
	МенеджерDBF.Записать();
КонецПроцедуры

//
// Выбираем фактический адрес, если его нет то юридический
//
Функция АдресКонтрагента(Контрагент)
	Адреса = РегистрыСведений.КонтактнаяИнформация.СоздатьНаборЗаписей();
	УправлениеКонтактнойИнформацией.ПрочитатьКонтактнуюИнформацию(Адреса, Контрагент.Ссылка);
	
	АдресСтр = "";
	
	Попытка
		Для каждого Адрес из Адреса Цикл
			Если Найти(Адрес.Вид.Наименование, "Фактический") > 0 Тогда
				Если Адрес.Представление <> "" Тогда
					Возврат Адрес.Представление;
				КонецЕсли;
				Возврат УправлениеКонтактнойИнформацией.ПолучитьПредставлениеАдреса(Адрес);
			КонецЕсли;
			Если Найти(Адрес.Вид.Наименование, "Юридический") > 0 Тогда
				Если Адрес.Представление <> "" Тогда
					АдресСтр = Адрес.Представление;
				Иначе
					АдресСтр = УправлениеКонтактнойИнформацией.ПолучитьПредставлениеАдреса(Адрес);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	Исключение
		Сообщить("ошибка АдресКонтрагента " + Контрагент + ": " + ОписаниеОшибки());
	КонецПопытки;
	
	Возврат АдресСтр;
КонецФункции

//
// Заполняем таблицу контактов для контрагента
//
Функция ЗаполнитьКонтакты(Контакты, Контрагент);
	кк = Справочники.КонтактныеЛицаКонтрагентов.Выбрать(,Контрагент.Ссылка);
	Телефоны = РегистрыСведений.КонтактнаяИнформация.СоздатьНаборЗаписей();
	
	Контакты.Очистить();
	Пока кк.Следующий() Цикл
		
		УправлениеКонтактнойИнформацией.ПрочитатьКонтактнуюИнформацию(Телефоны, кк.Ссылка);
		Телефон = "";
		
		// берем первый телефон, если он есть
		Если Телефоны.Количество() > 0 Тогда
			Телефон = Телефоны[0].Представление;
		КонецЕсли;
		
		Если Телефон <> "" Тогда
			Стр = Контакты.Добавить();
			Стр.ФИО = кк.КонтактноеЛицо.Фамилия + " " + кк.КонтактноеЛицо.Имя + " " + кк.КонтактноеЛицо.Отчество;	
			Стр.Телефон = Телефон;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Телефон;
КонецФункции

Процедура ВыгрузкаКонтрагентов(ТипыЦен)
	Сообщить("Выгружаем контрагентов по справочнику...");
		
	Менеджеры = ФайлМенеджеров();
	ТекстЗапроса =
	"ВЫБРАТЬ
	|	Контрагенты.ОсновнойМенеджерПокупателя.Ссылка КАК Агент,
	|	Контрагенты.Ссылка КАК Контрагент
	|ИЗ
	|	Справочник.Контрагенты КАК Контрагенты
	|
	|УПОРЯДОЧИТЬ ПО
	|	Агент";
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса;
	Выборка = Запрос.Выполнить().Выбрать();
		
	//
	// создаем таблицу значений для выгрузки контактов
	//
	Контакты = Новый ТаблицаЗначений;
	Контакты.Колонки.Добавить("ФИО", Новый ОписаниеТипов("Строка"));
	Контакты.Колонки.Добавить("Телефон", Новый ОписаниеТипов("Строка"));
	
	ВсеКАгенты = новый СписокЗначений;
	ВсеКА = ФайлОрганизаций(Истина);
	
	ТекАгент = "";
	ОргDBF = "";
	КонтDBF = "";
	Пока Выборка.Следующий() Цикл
		Если Выборка.Агент = Null или Выборка.Агент.Пустая() Тогда
			Продолжить;
		КонецЕсли;
		Если ТекАгент <> Выборка.Агент Тогда
			ЗакрытьФайлыАгента(ОргDBF, КонтDBF, ТекАгент);
			ТекАгент = Выборка.Агент;
			ОргDBF = ФайлОрганизаций();
            КонтDBF = ФайлКонтактов(ТекАгент);
		КонецЕсли;
		
		Контрагент = Выборка.Контрагент;
		Если Не Контрагент.Покупатель Тогда
			Продолжить;
		КонецЕсли;
		
		договор = Контрагент.ОсновнойДоговорКонтрагента;
		Если договор.ВидДоговора <> Перечисления.ВидыДоговоровКонтрагентов.СПокупателем Тогда
			Договоры = Справочники.ДоговорыКонтрагентов.Выбрать(,Контрагент);
			Пока Договоры.Следующий() Цикл
				Если Договоры.ПометкаУдаления Или Договоры.ВидДоговора <> Перечисления.ВидыДоговоровКонтрагентов.СПокупателем Тогда
					Продолжить;
				КонецЕсли;
				
				договор = Договоры.Ссылка;
				Прервать;
			КонецЦикла;
		КонецЕсли;
		
		кодОрг = Контрагент.Код;
		
		Адрес = АдресКонтрагента(Контрагент);
		
		ТелефонКА = ЗаполнитьКонтакты(Контакты, Контрагент);
				
		ПроверитьИнформациюПоМенеджеру(Менеджеры, ТекАгент);
		
		//
		// добавляем информацию по контрагентам и контактам
		//
		ОргDBF.Добавить();
		ОргDBF.ID = КодОрг;
		ОргDBF.NAME = Контрагент.Наименование;
		ОргDBF.ADDRESS = Адрес;
		//
		// ТипЦены для контрагента выгружаем как позиция цены в order.cfg
		//
		Если договор.ТипЦен <> Неопределено Тогда
			тц = договор.ТипЦен;
			Если ТипЗнч(тц) = Тип("СправочникСсылка.ТипыЦенНоменклатурыКонтрагентов") Тогда
				тц = тц.ТипЦеныНоменклатуры;	
			КонецЕсли;
			Если тц.Пустая() Тогда
				тц = ТипЦеныПрайс;
			КонецЕсли;
			
			типЦены = ТипыЦен.НайтиПоЗначению(тц);
			сч = 0;
			Если типЦены = Неопределено Тогда
				сч = ТипыЦен.Количество();
				ТипыЦен.Добавить(тц);
			Иначе
				сч = ТипыЦен.Индекс(типЦены);
			КонецЕсли;
			ОргDBF.COSTYPE = сч;
			ОргDBF.PRCTYPE = договор.ТипЦен.Код;
		Иначе	
			ОргDBF.PRCTYPE = ТипЦеныПрайс.Код;
		КонецЕсли;
		
		ОргDBF.Записать();		
		
		Если ВсеКАгенты.НайтиПоЗначению(Контрагент) = Неопределено ТОгда
			ВсеКАгенты.Добавить(Контрагент);
			ВсеКА.Добавить();
			ЗаполнитьЗначенияСвойств(ВсеКА, ОргDBF, "ID,ADDRESS,NAME,COSTYPE"); 
			ВсеКА.Записать();
		КонецЕсли;
		//
		// сохраняем информацию о контактах
		//
		Для Каждого Контакт из Контакты Цикл
			КонтDBF.Добавить();
			КонтDBF.ID = КодОрг;
			КонтDBF.FIO = Контакт.ФИО;
			КонтDBF.PHONE = Контакт.Телефон;
			КонтDBF.Записать();			
		КонецЦикла;			
	КонецЦикла;
	
	ЗакрытьФайлыАгента(ОргDBF, КонтDBF, ТекАгент);
	ВсеКА.ЗакрытьФайл();
	ПереместитьФайл(ПапкаОбмена + "\TORGS.DBF", ПапкаОбмена + "\ORGS.DBF");  
	ЗакрытьФайлМенеджеров(Менеджеры);
КонецПроцедуры

Процедура ВыгрузкаПапокТовара()
	склад = Справочники.Номенклатура.ВыбратьИерархически();	
	файлПапок = ПапкаОбмена +"\TFLD.DBF";
	папкаDBF = ФайлПапок(файлПапок);
	
	Пока склад.Следующий() Цикл
		Если склад.ПометкаУдаления Тогда
			Продолжить;
		КонецЕсли;

		Если ПустаяСтрока(склад.Наименование) Тогда
			Продолжить;
		КонецЕсли;
		
		Если Не склад.ЭтоГруппа Тогда
			Продолжить;
		КонецЕсли;
		
		папкаDBF.Добавить();
		папкаDBF.ID = склад.Код;
		папкаDBF.NAME = склад.Наименование;
		папкаDBF.LEVEL = склад.УровеньВВыборке();
		папкаDBF.Записать();
	КонецЦикла;

	папкаDBF.ЗакрытьФайл();
	ПереместитьФайл(файлПапок, ПапкаОбмена + "\FOLDERS.DBF"); 
КонецПроцедуры

Процедура ЗаполнитьРасчетныеЦены(стрТовара, цены, ВыгрЦены)
	перем ЦенаБазовая;
	Дата = ТекущаяДата();
	
	Если стрТовара = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	инд = 0;
	Для каждого стр из ВыгрЦены Цикл
		
		инд = инд + 1;
		
		ТипЦен = стр.Значение;
		Если Не (ТипЦен.Рассчитывается и ЗначениеЗаполнено(ТипЦен.БазовыйТипЦен)) Тогда
			Продолжить;
		КонецЕсли;
		
		ЦенаБазовая = цены.Получить(ТипЦен.БазовыйТипЦен);
		Если ЦенаБазовая = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		ПолученнаяЦена = 0;
		
		ВалютаТекущая = ТипЦен.ВалютаЦены;
		ВалютаБазовая = ТипЦен.БазовыйТипЦен.ВалютаЦены;
		
		Если ТипЦен.СпособРасчетаЦены = Перечисления.СпособыРасчетаЦены.ПоПроцентнойНаценкеНаБазовыйТип Тогда
			ПолученнаяЦена = ЦенаБазовая * (1 + стр.Значение.ПроцентСкидкиНаценки / 100);
			ВалютаТекущая = ВалютаБазовая;
		ИначеЕсли ТипЦен.СпособРасчетаЦены = Перечисления.СпособыРасчетаЦены.ПоВхождениюБазовойЦеныВДиапазон Тогда
			ПолученнаяЦена = Ценообразование.ПолучитьЦенуПоЦеновымДиапазонам(ТипЦен, Дата, ЦенаБазовая, ВалютаТекущая, ВалютаБазовая);
		КонецЕсли;
		
		ПолученнаяЦена = Ценообразование.ПересчитатьЦенуПриИзмененииВалюты(ПолученнаяЦена, ВалютаТекущая, ВалютаТекущая, 0, 1, Дата);
		ПолученнаяЦена = Ценообразование.ОкруглитьЦену(ПолученнаяЦена, ТипЦен.ПорядокОкругления, ТипЦен.ОкруглятьВБольшуюСторону);
		
		стрТовара.Цены.Вставить("COST" + Строка(инд), ПолученнаяЦена);
	КонецЦикла;
КонецПроцедуры

Функция ВыгрузитьТовары(ТипыЦен)
	ВыгрЦены = ТипыЦен.Скопировать();
	
	
	Для каждого стр из ВыгрЦены Цикл
		ТипЦены = стр.Значение;
		Если (ТипЦены.Рассчитывается и ЗначениеЗаполнено(ТипЦены.БазовыйТипЦен)) Тогда
			эл = ВыгрЦены.НайтиПоЗначению(ТипЦены.БазовыйТипЦен);
			Если эл = Неопределено Тогда
				ВыгрЦены.Добавить(ТипЦены.БазовыйТипЦен);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	таблТоваров = Новый	ТаблицаЗначений;
	таблТоваров.Колонки.Добавить("Товар");
	таблТоваров.Колонки.Добавить("ОстатокОбщие");
	таблТоваров.Колонки.Добавить("ОстатокМирМасел");
	таблТоваров.Колонки.Добавить("Цены");
	таблТоваров.Колонки.Добавить("МинЦена");
	
	ТекстЗапроса =
	"ВЫБРАТЬ
	|	ЦеныНоменклатуры.Цена,
	|	ЦеныНоменклатуры.Номенклатура.Ссылка КАК Товар,
	|	ЦеныНоменклатуры.ТипЦен.Ссылка КАК ТипЦены,
	|	ЦеныНоменклатуры.ЕдиницаИзмерения,
	|	ЕСТЬNULL(ВложенныйЗапрос.ОстатокОбщие, 0) КАК ОстатокОбщие,
	|	ЕСТЬNULL(ВложенныйЗапрос.ОстатокМирМасел, 0) КАК ОстатокМирМасел
	|ИЗ
	|	РегистрСведений.ЦеныНоменклатуры.СрезПоследних(&Дата, ТипЦен.Ссылка В (&ТипыЦен)) КАК ЦеныНоменклатуры
	|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
	|			ЕСТЬNULL(ОстаткиОбщие.КоличествоОстаток, 0) КАК ОстатокОбщие,
	|			ЕСТЬNULL(ОстаткиОбщие.Номенклатура, ОстаткиМирМасел.Номенклатура) КАК Номенклатура,
	|			ЕСТЬNULL(ОстаткиМирМасел.КоличествоОстаток, 0) КАК ОстатокМирМасел
	|		ИЗ
	|			РегистрНакопления.ТоварыНаСкладах.Остатки(&Дата, Склад = &СкладМирМасел) КАК ОстаткиМирМасел
	|				ПОЛНОЕ СОЕДИНЕНИЕ РегистрНакопления.ТоварыНаСкладах.Остатки(&Дата, Склад = &СкладОстатков) КАК ОстаткиОбщие
	|				ПО ОстаткиМирМасел.Номенклатура = ОстаткиОбщие.Номенклатура) КАК ВложенныйЗапрос
	|		ПО ЦеныНоменклатуры.Номенклатура = ВложенныйЗапрос.Номенклатура
	|
	|УПОРЯДОЧИТЬ ПО
	|	Товар";
	
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса;
	Запрос.Параметры.Вставить("ТипыЦен", ВыгрЦены);
	Запрос.Параметры.Вставить("Дата", ТекущаяДата());
	Запрос.Параметры.Вставить("СкладОстатков", СкладОстатков);
	Запрос.Параметры.Вставить("СкладМирМасел", СкладМирМасел);
	
	базовыеЦены = Новый Соответствие;
	текТовар = Неопределено;
	стрТовара = Неопределено;
	рез = Запрос.Выполнить().Выгрузить(ОбходРезультатаЗапроса.Прямой);	
	Для Каждого стр из рез Цикл
		Если стр.Товар.ПометкаУдаления Тогда
			Продолжить;
		КонецЕсли;
		
		Если текТовар <> стр.Товар Тогда
			ЗаполнитьРасчетныеЦены(стрТовара, базовыеЦены, ТипыЦен);

			текТовар = стр.Товар;
			
			стрТовара = таблТоваров.Добавить();
			стрТовара.Товар = стр.Товар;
			стрТовара.ОстатокМирМасел = стр.ОстатокМирМасел;
			стрТовара.ОстатокОбщие = стр.ОстатокОбщие;
			стрТовара.Цены = Новый Соответствие;
			//Сообщить("== "+текТовар);
		КонецЕсли;
		
		базовыеЦены.Вставить(стр.ТипЦены, стр.Цена);
		Цена = стр.Цена;
		Если стр.ЕдиницаИзмерения.Коэффициент <> 0 Тогда
			Цена = Цена / стр.ЕдиницаИзмерения.Коэффициент;
		КонецЕсли;
		
		типЦены = ТипыЦен.НайтиПоЗначению(стр.ТипЦены);		
		Если типЦены <> Неопределено Тогда
			стр = ТипыЦен.Индекс(типЦены) + 1;
			стрТовара.Цены.Вставить("COST" + Строка(стр), Цена);
		КонецЕсли;
	КонецЦикла;
	
	ЗаполнитьРасчетныеЦены(стрТовара, базовыеЦены, ТипыЦен);
	
	Возврат таблТоваров;
КонецФункции

//
// Выгружаем остатки товара и цены
//
Процедура ВыгрузкаТовара(ТипыЦен)
	Перем цена, едЦены;
	
	Сообщить("Выгружаем информацию по номенклатуре...");
	    	
	товары = ВыгрузитьТовары(ТипыЦен);
	
	Лукойл = Новый СписокЗначений;
	Для каждого стрПапка из ПапкиЛукойл Цикл
		Лукойл.Добавить(стрПапка.ПапкаЛукйол);
	КонецЦикла;
	
	ТоварыЛукйол = новый СписокЗначений;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Номенклатура.Код
	|ИЗ
	|	Справочник.Номенклатура КАК Номенклатура
	|ГДЕ
	|	Номенклатура.Ссылка В ИЕРАРХИИ(&Лукойл)";
	Запрос.Параметры.Вставить("Лукойл", Лукойл);
	выб = Запрос.Выполнить().Выбрать();
	Пока выб.Следующий() Цикл
		ТоварыЛукйол.Добавить(выб.Код);
	КонецЦикла;
	
	
	//Для Каждого стр из АгентыСклады Цикл
	//	Если стр.Агент.Пустая() или стр.Склад.Пустая() Тогда
	//		Продолжить;
	//	КонецЕсли;
	//	
	//	ВыгрузитьТоварыАгента(стр, товары, ВыгрЦены);
	//КонецЦикла;
	
	// Выгрузим общий прайс
	ВыгрузитьТоварыАгента(Неопределено, товары, ТоварыЛукйол, ТипыЦен);
	ВыгрузкаПапокТовара();
КонецПроцедуры

Процедура ВыгрузитьТоварыАгента(СтрАгент, товары, ТоварыЛукйол, ВыгрЦены)
	
	файлТовара = ПапкаОбмена + "\TWH.DBF";
	Остатки = РегистрыНакопления.ТоварыНаСкладах;
	складDBF = ФайлТоваров(файлТовара, ВыгрЦены);
					
	Для Каждого стр из Товары Цикл
		текТовар = стр.Товар;
		
		
		складDBF.Добавить();
		
		Кол = стр.ОстатокОбщие;
		Кол2 = стр.ОстатокМирМасел;
		складDBF.ID = текТовар.Код;
		складDBF.FOLDER = текТовар.Родитель.Код;
		складDBF.NAME = текТовар.Наименование;
		Если Не текТовар.ЕдиницаХраненияОстатков.Пустая() Тогда
			складDBF.INPACK = текТовар.ЕдиницаХраненияОстатков.Коэффициент;
			Кол = Кол * текТовар.ЕдиницаХраненияОстатков.Коэффициент;
			Кол2 = Кол2 * текТовар.ЕдиницаХраненияОстатков.Коэффициент;
		Иначе
			складDBF.INPACK = 1;
		КонецЕсли;
		
		складDBF.QTY = Кол + Кол2;
		складDBF.QTY2 = Кол + Кол2;
		//складDBF.QTY2 = Кол2;
		складDBF.PRT = текТовар.Признак;
		складDBF.ISLUKOIL = ?(ТоварыЛукйол.НайтиПоЗначению(текТовар.код) <> Неопределено, 1, 0);
		складDBF.NODSC = текТовар.СкидкиНет;
		
		Для Каждого цена Из стр.Цены Цикл
			складDBF.УстановитьЗначениеПоля(цена.Ключ, цена.Значение);		
		КонецЦикла;
		складDBF.Записать();	
	КонецЦикла;

	складDBF.ЗакрытьФайл();
	
	ПереместитьФайл (файлТовара, ПапкаОбмена + ?(СтрАгент = Неопределено, "\WAREHOUS.DBF", "\W" + КодАгента(СтрАгент.Агент) + ".DBF")); 

КонецПроцедуры

Процедура ДобавитьФирму(фмт, фирма, тип)
	ки = РегистрыСведений.КонтактнаяИнформация.СоздатьНаборЗаписей();
	фмт.Добавить();
	фмт.ID = фирма.Код;
	фмт.NAME = фирма.Наименование;
	фмт.TYPE = тип;
	
	фмт.Записать();

КонецПроцедуры 

//
// сохранить информацию о фирме и типах цен в order.cfg
// колвоЦен = определяет сколько цен выгружать для товаров
//
Процедура ВыгрузкаНастроек(ТипыЦен)
	конфиг = Новый ТекстовыйДокумент;
	конфиг.Очистить();
	
	//	
	// выгружаем фирмы
	//

	фмт = ФайлФирм();
	Если Не ОрганизацияНаличныйРасчет.Пустая() Тогда
		ДобавитьФирму(фмт, ОрганизацияНаличныйРасчет, 0);
	КонецЕсли;
	Если Не ОрганиацияЛукойл.Пустая() Тогда
		ДобавитьФирму(фмт, ОрганиацияЛукойл, 1);
	КонецЕсли;
	Если Не ОрганиацияОстальное.Пустая() Тогда
		ДобавитьФирму(фмт, ОрганиацияОстальное, 2);
	КонецЕсли;
	ЗакрытьФайлФирм(фмт);
	
	//
	// записываем типы цен
	//
	кфгСтр = "ВидЦены=";
	Для Каждого стр из ТипыЦен Цикл
		кфгСтр = кфгСтр + стр.Значение.Наименование + Символ(9) + стр.Значение.Код + ";";
	КонецЦикла;	
	конфиг.ДобавитьСтроку(Лев(кфгСтр, СтрДлина(кфгСтр)-1));
	
	Если ИзменятьТипЦены = 1 Тогда
		конфиг.ДобавитьСтроку("МожноИзменятьЦену=1");
	Иначе
		конфиг.ДобавитьСтроку("МожноИзменятьЦену=0");
	КонецЕсли;
	
	конфиг.Записать(ПапкаОбмена + "\order.cfg", "windows-1251");
КонецПроцедуры

//
// Выгружается только последние 5 символов кода пользователя
//
Функция НайтиАгента(КодАгента)
	уид = Новый УникальныйИдентификатор(КодАгента);
	Возврат Справочники.Пользователи.ПолучитьСсылку(уид);
КонецФункции

Функция СоздатьЗаказ(Заказ, Агент, можетМенятьЦену)
	
	параметрыДоговора = ЗаполнениеДокументов.ПолучитьСтруктуруПараметровДляПолученияДоговораЗаказаПокупателя();
	
	Накладная = Документы.РеализацияТоваровУслуг.СоздатьДокумент();
	
	датаЗаказа = Формат(Заказ.DATE, "ДФ=ггггММдд");
	текДата = Формат(ТекущаяДата(), "ДФ=ггггММдд");
	текВремя = Формат(ТекущаяДата(), "ДФ=ЧЧммсс");
	
	Если датаЗаказа < текДата Тогда
		// дата меньше текущей - используем текущую
		датаЗаказа = текДата;
	КонецЕсли;
	
	Если (датаЗаказа = текДата и текВремя > "170000") Тогда
		// сегодня после 17. Заказ на завтра
		датаЗаказа = Формат((ТекущаяДата() + 24 * 3600), "ДФ=ггггММдд");
		текВремя = "080000";
	КонецЕсли;
	
	стрдата = датаЗаказа + текВремя;
	Накладная.Дата = Дата(стрдата);
	Накладная.ВидОперации = Перечисления.ВидыОперацийРеализацияТоваров.ПродажаКомиссия;
	Накладная.ВидПередачи = Перечисления.ВидыПередачиТоваров.СоСклада;
	
	
	Если Заказ.USETAX <> 0 и Заказ.CASH = 0 Тогда
		Накладная.Организация = ОрганиацияОстальное;			
		Накладная.Склад = СкладМирМасел;
	Иначе
		Накладная.Организация = ОрганизацияНаличныйРасчет;			
		Накладная.Склад = СкладОстатков;
	КонецЕсли;
	
	//организация = Справочники.Организации.НайтиПоКоду(Заказ.FIRMCODE);
	//Если организация <> Неопределено Тогда
	//	Накладная.Организация = организация;
	//КонецЕсли;
	//
	//НДС = Ложь;
	//Накладная.Склад = СкладОстатков;
	//Если Заказ.CASH = 0 Тогда
	//	Если Заказ.USETAX <> 0 Тогда
	//		НДС = Истина;
	//		Если Заказ.LUKOIL = 0 Тогда
	//			Накладная.Склад = СкладМирМасел;	
	//		КонецЕсли;
	//	КонецЕсли;
	//КонецЕсли;
	Накладная.УчитыватьНДС = Истина;
	Накладная.СуммаВключаетНДС = Истина;
	Накладная.ОтражатьВУправленческомУчете = Истина;
	
	
	контрагент = Справочники.Контрагенты.НайтиПоКоду(Заказ.ID);
	Накладная.Грузополучатель = контрагент;
	Накладная.Контрагент = контрагент;
	
	договор = Контрагент.ОсновнойДоговорКонтрагента;
	Если договор.ВидДоговора <> Перечисления.ВидыДоговоровКонтрагентов.СПокупателем Тогда
		договор = Справочники.ДоговорыКонтрагентов.ПустаяСсылка();
		
		Договоры = Справочники.ДоговорыКонтрагентов.Выбрать(,Контрагент);
		Пока Договоры.Следующий() Цикл
			Если Договоры.ПометкаУдаления Или Договоры.ВидДоговора <> Перечисления.ВидыДоговоровКонтрагентов.СПокупателем Тогда
				Продолжить;
			КонецЕсли;
			
			договор = Договоры.Ссылка;
			Прервать;
		КонецЦикла;
	КонецЕсли;
	
	Накладная.ДоговорКонтрагента = договор;
	Накладная.ТипЦен = Накладная.ДоговорКонтрагента.ТипЦен;
	
	Если можетМенятьЦену Тогда
		типЦен = Справочники.ТипыЦенНоменклатуры.НайтиПоКоду(Заказ.PRCTYPE);
		Если типЦен <> Неопределено Тогда
			Накладная.ТипЦен = типЦен;	
		КонецЕсли;
	КонецЕсли;
	
	ЗаполнениеДокументов.ПриИзмененииЗначенияКонтрагента(Накладная, параметрыДоговора);
	
	Если ЗначениеЗаполнено(Накладная.ДоговорКонтрагента) Тогда
		Накладная.ВалютаДокумента = Накладная.ДоговорКонтрагента.ВалютаВзаиморасчетов;
	Иначе
		Накладная.ВалютаДокумента = глЗначениеПеременной("ВалютаРегламентированногоУчета");;
	КонецЕсли;
	Курс = МодульВалютногоУчета.ПолучитьКурсВалюты(Накладная.ВалютаДокумента, Накладная.Дата);
	Накладная.КурсВзаиморасчетов = Курс.Курс;
	Накладная.КратностьВзаиморасчетов = Курс.Кратность;
	//УправлениеЗаказами.УстановитьДатуОплатыПоДоговору(Накладная,"",Накладная.ДоговорКонтрагента);
	
	Если Агент <> Неопределено Тогда
		Накладная.Ответственный = Агент;
	КонецЕсли;
	
	Накладная.Комментарий = Заказ.REMARK;
	Накладная.БанковскийСчетОрганизации = Накладная.Организация.ОсновнойБанковскийСчет;	
	
	Возврат Накладная;
КонецФункции

Процедура ПриемЗаявокНажатие(Элемент)
	ПриемЗаявок();
КонецПроцедуры

Процедура ВыгрузкаОстатковНажатие(Элемент)
	ВыгрузкаОстатков();	
КонецПроцедуры

Процедура СохранитьНакладную(накладная, Документ, СуммаДолгаДок, НомерЗаказа)
	
	Для Каждого товар из Документ.Товары Цикл
		накладная.Добавить();
		накладная.ID = Документ.Контрагент.Код;
		накладная.NUM = Документ.Номер;
		накладная.DATE = Документ.Дата;
		накладная.ID_I = товар.Номенклатура.Код;
		накладная.QTY = товар.Количество;
		накладная.SUM = товар.Сумма * Документ.КурсВзаиморасчетов;
		накладная.SUMD = СуммаДолгаДок;
		накладная.PAYDATE = Документ.Дата + Документ.ДоговорКонтрагента.ДопустимоеЧислоДнейЗадолженности * 24 * 3600;
		накладная.CREATED = НомерЗаказа;
		накладная.Записать();
	КонецЦикла;
КонецПроцедуры

Процедура ВыгрузкаНакладныхНажатие(Элемент)
	ВыгрузкаДолгов();	
КонецПроцедуры

Процедура ВыгрузкаДневныхНакладных() Экспорт
    Сообщить("Выгрузка отгрузок и долгов...");
	
	ДатаКон = ТекущаяДата();
	ДатаНач = НачалоДня(ДатаКон);
	
	
	ТекстЗапроса = 
	"ВЫБРАТЬ
	|	РеализацияТоваровУслуг.Ссылка КАК Документ,
	|	РеализацияТоваровУслуг.Контрагент.ОсновнойМенеджерПокупателя.Ссылка КАК Агент,
	|	РеализацияТоваровУслуг.Контрагент КАК Контрагент
	|ИЗ
	|	Документ.РеализацияТоваровУслуг КАК РеализацияТоваровУслуг
	|ГДЕ
	|	РеализацияТоваровУслуг.Дата МЕЖДУ &ДатаНач И &ДатаКон
	|
	|УПОРЯДОЧИТЬ ПО
	|	Агент,
	|	Контрагент,
	|	РеализацияТоваровУслуг.Дата УБЫВ";
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса;
	Запрос.Параметры.Вставить("ДатаКон", ДатаКон);
	Запрос.Параметры.Вставить("ДатаНач", ДатаНач);

	
	отгрузки = Неопределено;
	ПринятыеЗаявки= Неопределено;
	
	текАгент = Неопределено;
	
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Если Выборка.Агент = Null или Выборка.Агент.Пустая() Тогда
			Продолжить;
		КонецЕсли;
		
		Если текАгент <> Выборка.Агент Тогда
			
			ЗакрытьОплатыОтгрузки(Неопределено, отгрузки, текАгент,Истина);
			
			текАгент = Выборка.Агент;
			отгрузки = ФайлОтгрузок(Истина);
			ПринятыеЗаявки = ЗагрузитьПринятыеДокументы(КодАгента(текАгент),Ложь);
		КонецЕсли;
		
		НомерЗаказа = ПринятыеЗаявки.НайтиПоЗначению(СокрЛП(выборка.Документ.Номер));
		Если НомерЗаказа <> Неопределено Тогда
			НомерЗаказа = НомерЗаказа.Представление;
		Иначе
			НомерЗаказа = "";
		КонецЕсли;
		
		СохранитьНакладную(отгрузки, Выборка.Документ, 0, НомерЗаказа);	
	КонецЦикла;	
	
	ЗакрытьОплатыОтгрузки(Неопределено, отгрузки, текАгент,Истина);
	Сообщить("ОК");

КонецПроцедуры


Процедура ВыгрузкаДневныхНакладныхНажатие(Элемент)
	ВыгрузкаДневныхНакладных();	
КонецПроцедуры

Процедура НастройкиПоУмолчанию()
	ДатаКон = ТекущаяДата();
	ДатаНач = ДобавитьМесяц(ДатаКон, -2);
	
	Склады = Справочники.Склады.Выбрать();
	Если Склады.Следующий() Тогда
		СкладОстатков = Склады.Ссылка;
	КонецЕсли;
	
	ГлубинаВыгрузкиДолгов = 1;
	ИзменятьТипЦены = Истина;
	
	СтатьяДвижения = Справочники.СтатьиДвиженияДенежныхСредств.НайтиПоКоду("000000002");
КонецПроцедуры

Процедура ЗагрузитьНастройки()	
	текЗнач = ВосстановитьЗначение("СкладОстатков");
	Если текЗнач <> Неопределено Тогда
		СкладОстатков = текЗнач;
	КонецЕсли;
	
	текЗнач = ВосстановитьЗначение("ИзменятьТипЦены");
	Если текЗнач <> Неопределено Тогда
		ИзменятьТипЦены = текЗнач;
	КонецЕсли;	
	
	текЗнач = ВосстановитьЗначение("ПапкаОбмена");
	Если текЗнач <> Неопределено Тогда
		ПапкаОбмена = текЗнач;
	КонецЕсли;	
	
	текЗнач = ВосстановитьЗначение("ГлубинаВыгрузкиДолгов");
	Если текЗнач <> Неопределено Тогда
		ГлубинаВыгрузкиДолгов = текЗнач;
	КонецЕсли;
	
	текЗнач = ВосстановитьЗначение("ТипЦеныПрайс");
	Если текЗнач <> Неопределено Тогда
		ТипЦеныПрайс = текЗнач;
	КонецЕсли;
	
	текЗнач = ВосстановитьЗначение("СкладМирМасел");
	Если текЗнач <> Неопределено Тогда
		СкладМирМасел = текЗнач;
	КонецЕсли;
	
	текЗнач = ВосстановитьЗначение("ОрганизацияНаличныйРасчет");
	Если текЗнач <> Неопределено Тогда
		ОрганизацияНаличныйРасчет = текЗнач;
	КонецЕсли;
	
	текЗнач = ВосстановитьЗначение("ОрганиацияЛукойл");
	Если текЗнач <> Неопределено Тогда
		ОрганиацияЛукойл = текЗнач;
	КонецЕсли;
	
	текЗнач = ВосстановитьЗначение("ОрганиацияОстальное");
	Если текЗнач <> Неопределено Тогда
		ОрганиацияОстальное = текЗнач;
	КонецЕсли;
	
	текЗнач = ВосстановитьЗначение("ПапкиЛукойл");
	Если текЗнач <> Неопределено Тогда
		ПапкиЛукойл = текЗнач;
	КонецЕсли;
КонецПроцедуры

Процедура СохранитьНастройки()
	СохранитьЗначение("СкладОстатков", СкладОстатков);
	СохранитьЗначение("ИзменятьТипЦены", ИзменятьТипЦены);
	СохранитьЗначение("ТипЦеныПрайс", ТипЦеныПрайс);
	СохранитьЗначение("ГлубинаВыгрузкиДолгов", ГлубинаВыгрузкиДолгов);
	СохранитьЗначение("ПапкаОбмена", ПапкаОбмена);
	
	СохранитьЗначение("СкладМирМасел", СкладМирМасел);
	
	СохранитьЗначение("ОрганизацияНаличныйРасчет", ОрганизацияНаличныйРасчет);
	СохранитьЗначение("ОрганиацияЛукойл", ОрганиацияЛукойл);
	СохранитьЗначение("ОрганиацияОстальное", ОрганиацияОстальное);
	
	СохранитьЗначение("ПапкиЛукойл", ПапкиЛукойл);
КонецПроцедуры

Процедура ПапкаОбменаНачалоВыбора(Элемент, СтандартнаяОбработка)
    СтандартнаяОбработка = Ложь;

	Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.ВыборКаталога);    
    Диалог.Заголовок = "Выберите папку обмена";

	Диалог.Каталог = ПапкаОбмена;
    Если Диалог.Выбрать() Тогда
        ПапкаОбмена = Диалог.Каталог;
	КонецЕсли;
КонецПроцедуры

Процедура ПриЗакрытии()
	СохранитьНастройки();
КонецПроцедуры

Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)	
	НастройкиПоУмолчанию();
	ЗагрузитьНастройки();
КонецПроцедуры

Процедура ВыгрузкаОстатков() Экспорт
	ТипыЦен = Новый СписокЗначений;
	
	Если Не ТипЦеныПрайс.Пустая() Тогда
		ТипыЦен.Добавить(ТипЦеныПрайс);
	КонецЕсли;
	
	ВыгрузкаКонтрагентов(ТипыЦен);
	ВыгрузкаТовара(ТипыЦен);
	ВыгрузкаНастроек(ТипыЦен);
	
	Сообщить("ОК");
КонецПроцедуры

Функция ВыгрузитьДолги(Дата)
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ВзаиморасчетыСКонтрагентамиОстатки.Контрагент,
	|	ВзаиморасчетыСКонтрагентамиОстатки.СуммаУпрОстаток КАК Сумма,
	|	ВзаиморасчетыСКонтрагентамиОстатки.Контрагент.ОсновнойМенеджерПокупателя КАК Агент
	|ИЗ
	|	РегистрНакопления.ВзаиморасчетыСКонтрагентами.Остатки(&Дата, ) КАК ВзаиморасчетыСКонтрагентамиОстатки";
	
	Запрос.Параметры.Вставить("Дата", Дата);
	
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

Процедура ВыгрузитьДолгиБезДвижения(долги, долгиКА, текАгент, ДатаНач)
	Если текАгент <> Неопределено Тогда
		отбор = Новый Структура("Агент", текАгент);
		стрДолги = долгиКА.НайтиСтроки(отбор);
		
		Для каждого стр из стрДолги Цикл
			Если стр.Сумма <> 0 Тогда
				долги.Добавить();
				долги.ID = стр.Контрагент.Код;
				долги.DATE = ДатаНач;
				долги.SUM = стр.Сумма;
				долги.NUM = "Нач.сальдо";				
				долги.Записать();
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;			
КонецПроцедуры

Процедура ВыгрузкаДолгов() Экспорт
    Сообщить("Выгрузка отгрузок и долгов...");
	
	ДатаКон = ТекущаяДата();
	ДатаНач = ДобавитьМесяц(ДатаКон, -ГлубинаВыгрузкиДолгов);
	
	
	ТекстЗапроса = 
	"ВЫБРАТЬ
	|	РеализацияТоваровУслуг.Ссылка КАК Документ,
	|	РеализацияТоваровУслуг.Контрагент.ОсновнойМенеджерПокупателя.Ссылка КАК Агент,
	|	РеализацияТоваровУслуг.Контрагент КАК Контрагент
	|ИЗ
	|	Документ.РеализацияТоваровУслуг КАК РеализацияТоваровУслуг
	|ГДЕ
	|	РеализацияТоваровУслуг.Дата МЕЖДУ &ДатаНач И &ДатаКон
	|
	|УПОРЯДОЧИТЬ ПО
	|	Агент,
	|	Контрагент,
	|	РеализацияТоваровУслуг.Дата УБЫВ";
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса;
	Запрос.Параметры.Вставить("ДатаКон", ДатаКон);
	Запрос.Параметры.Вставить("ДатаНач", ДатаНач);

	
	долги = Неопределено;
	отгрузки = Неопределено;
	ПринятыеЗаявки= Неопределено;
	
	текАгент = Неопределено;
	текКА = Неопределено;
	СуммаДолга = 0;
	
	долгиКА = ВыгрузитьДолги(ДатаКон);

	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Если Выборка.Агент = Null или Выборка.Агент.Пустая() Тогда
			Продолжить;
		КонецЕсли;
		
		Если текАгент <> Выборка.Агент Тогда
			Если СуммаДолга <> 0 Тогда
				долги.Добавить();
				долги.ID = текКА.Код;
				долги.DATE = ДатаНач;
				долги.SUM = СуммаДолга;
				долги.NUM = "Нач.сальдо";				
				долги.Записать();
				СуммаДолга = 0;
			КонецЕсли;
			
			ВыгрузитьДолгиБезДвижения(долги, долгиКА, текАгент, ДатаНач);
			
			ЗакрытьОплатыОтгрузки(долги, отгрузки, текАгент, Ложь);
			
			текАгент = Выборка.Агент;
			текКА = Неопределено;
			
			долги = ФайлОплат();
			отгрузки = ФайлОтгрузок(Ложь);
			ПринятыеЗаявки = ЗагрузитьПринятыеДокументы(КодАгента(текАгент), Ложь);
		КонецЕсли;
		
		Если текКА <> Выборка.Контрагент Тогда
			Если СуммаДолга <> 0 Тогда
				долги.Добавить();
				долги.ID = текКА.Код;
				долги.DATE = ДатаНач;
				долги.SUM = СуммаДолга;
				долги.NUM = "Нач.сальдо";				
				долги.Записать();
			КонецЕсли;
			текКА = Выборка.Контрагент;
			СуммаДолга = 0;
			отбор = Новый Структура("Контрагент", текКА);
			стрДолг = долгиКА.НайтиСтроки(отбор);
			Если стрДолг.Количество() > 0 Тогда
				СуммаДолга = стрДолг[0].Сумма;
				долгиКА.Удалить(стрДолг[0]);
			КонецЕсли;
		КонецЕсли;
				
		НомерЗаказа = ПринятыеЗаявки.НайтиПоЗначению(СокрЛП(выборка.Документ.Номер));
		Если НомерЗаказа <> Неопределено Тогда
			НомерЗаказа = НомерЗаказа.Представление;
		Иначе
			НомерЗаказа = "";
		КонецЕсли;
		
		СуммаДокДолг = выборка.Документ.СуммаДокумента;
		Если СуммаДолга < СуммаДокДолг Тогда
			СуммаДокДолг = СуммаДолга;
		КонецЕсли;
		СуммаДолга = СуммаДолга - СуммаДокДолг;
		СохранитьНакладную(отгрузки, Выборка.Документ, СуммаДокДолг, НомерЗаказа);	
	КонецЦикла;	
	
	Если СуммаДолга <> 0 Тогда
		долги.Добавить();
		долги.ID = текКА.Код;
		долги.DATE = ДатаНач;
		долги.SUM = СуммаДолга;
		долги.NUM = "Нач.сальдо";				
		долги.Записать();
	КонецЕсли;
	ВыгрузитьДолгиБезДвижения(долги, долгиКА, текАгент, ДатаНач);
	ЗакрытьОплатыОтгрузки(долги, отгрузки,текАгент, Ложь);
	Сообщить("ОК");

КонецПроцедуры

Процедура ЗакрытьФайлОбработанныхЗаявок(таблица, КодСотр)
	таблица.ЗакрытьФайл();
	имяФайла = ПапкаОбмена + "\POD" + СокрЛП(КодСотр) + ".DBF";
	темпФайл = ПапкаОбмена + "\TPOD.DBF";
	ПереместитьФайл(темпФайл, имяФайла);
КонецПроцедуры

Процедура СохранитьПринятыеДокументы(КодАгента, списокЗаявок, ТипДок = Неопределено)
	имяФайла = ПапкаОбмена + "\TPRD.DBF";
	
	ДБФ = Новый XBase();
	ДБФ.Поля.Добавить("CREATED",   "S", 20, 0);
	ДБФ.Поля.Добавить("NUMBER", "S", 20, 0);
	ДБФ.СоздатьФайл(имяФайла);
	ДБФ.Кодировка = КодировкаXBase.OEM;
	
	ДатаНач = ДобавитьМесяц(ТекущаяДата(), -ГлубинаВыгрузкиДолгов);
	нач = Формат(ДатаНач, "ДФ=""ггггММдд000000""");
	
	Для Каждого стр из списокЗаявок Цикл
		Если стр.Значение > нач Тогда
			ДБФ.Добавить();
			ДБФ.CREATED = стр.Значение;
			ДБФ.NUMBER = стр.Представление;
			ДБФ.Записать();         
		КонецЕсли;
	КонецЦикла;
	
	ДБФ.ЗакрытьФайл();
	Если ТипДок = "ПКО" Тогда
		новФайл = ПапкаОбмена + "\OKD" + КодАгента + ".DBF";
	Иначе
		новФайл = ПапкаОбмена + "\PRD" + КодАгента + ".DBF";
	КонецЕсли;
	ПереместитьФайл(имяФайла, новФайл);
КонецПроцедуры

Функция ЗагрузитьПринятыеДокументы(КодАгента, ПоЗаявкам, ТипДок = Неопределено)
	Если ТипДок = "ПКО" Тогда
		имяФайла = ПапкаОбмена + "\OKD" + КодАгента + ".DBF";
	Иначе
		имяФайла = ПапкаОбмена + "\PRD" + КодАгента + ".DBF";
	КонецЕсли;
	
	файл = Новый Файл(имяФайла);

	списокЗаявок = Новый СписокЗначений;
	
	Если файл.Существует() Тогда
		КопироватьФайл (имяФайла, ПапкаОбмена + "\TPRD.DBF");
		ДБФ = Новый XBase();
		ДБФ.ОткрытьФайл(ПапкаОбмена + "\TPRD.DBF");
		ДБФ.Кодировка = КодировкаXBase.OEM;
		
		ДБФ.Первая();
		Пока Не ДБФ.ВКонце() Цикл
			Если ПоЗаявкам Тогда
				списокЗаявок.Добавить(СокрЛП(ДБФ.CREATED), СокрЛП(ДБФ.NUMBER));
			Иначе
				списокЗаявок.Добавить(СокрЛП(ДБФ.NUMBER), СокрЛП(ДБФ.CREATED));
			КонецЕсли;
			ДБФ.Следующая();
		КонецЦикла;
		
		ДБФ.ЗакрытьФайл();
	КонецЕсли;
	
	Возврат списокЗаявок;
КонецФункции

Функция ПризнакДоговора(Договор)
	Признак = Договор.Наименование;
	поз = СтрДлина(Признак);
	Пока поз > 1 Цикл
		Если Сред(Признак, поз, 1) = ")" Тогда
			 поз2 = поз - 1;
			Пока поз2 > 1 Цикл
				Если Сред(Признак, поз2, 1) = "(" Тогда
					Признак = Сред(Признак, поз2 + 1, СтрДлина(Признак) - поз + 1);
					Возврат СокрЛП(Признак);
				КонецЕсли;
				поз2 = поз2 - 1;
			КонецЦикла;
		КонецЕсли;
		поз = поз - 1;
	КонецЦикла;
	Возврат "";
КонецФункции

Функция НайтиДоговор(Контрагент, Грузополучатель, ПризнакСклада, ПризнакДог)
	текДоговор = Неопределено;
	
	неСмотретьГП = (Контрагент = Грузополучатель);
	Договора = Справочники.ДоговорыКонтрагентов.Выбрать(,Контрагент);
	Пока договора.Следующий() Цикл
		// предполагаем, что признак стоит только на одном договоре
		Если (неСмотретьГП или договора.КонтрагентТорговаяТочка = Грузополучатель) и 
				договора.ПризнакСклада = ПризнакСклада и
				ПризнакДоговора(договора.Ссылка) = ПризнакДог Тогда
			текДоговор = договора.Ссылка;
			Прервать;	
		КонецЕсли;
	КонецЦикла;
	
	Возврат текДоговор;
КонецФункции

Процедура ЗаписатьВЛог(текст)
	имяФайла = ПапкаОбмена + "\\log.txt";
	док = Новый ТекстовыйДокумент;
	файл = Новый Файл(имяФайла);
	Если файл.Существует() Тогда
		док.Прочитать(имяФайла, КодировкаТекста.ANSI);
	КонецЕсли;
	док.ДобавитьСтроку(Строка(ТекущаяДата()) + " "  + текст);
	док.Записать(имяФайла, КодировкаТекста.ANSI);
КонецПроцедуры

Процедура Ожидание(Таймаут)
    ВремяЗавершения = CurrentDate() + Таймаут;
    While CurrentDate() < ВремяЗавершения Do
        Try
            Соединение = New FTPConnection("127.0.0.1", 22587);
        Except
        EndTry;                    
    EndDo;
КонецПроцедуры

Функция ПопыткаЗаписатьДокумент(док, режим)
	рет = Ложь;
	
	сч = 0;
	
	ош = "";
	Пока сч < 3 и рет = Ложь Цикл
		Попытка
			//Если режим = РежимЗаписиДокумента.Проведение Тогда
			//	док.Записать(режим, РежимПроведенияДокумента.Оперативный);
			//Иначе
				док.Записать(режим);
			//КонецЕсли;
			рет = Истина;
			Прервать;
		Исключение
			ош = ОписаниеОшибки();
		КонецПопытки;
		
		Ожидание(4);		
		сч = сч + 1;
	КонецЦикла;
	
	Если не рет и СтрДлина(ош) > 0 Тогда
		ЗаписатьВЛог(ош);
		#если клиент тогда
		Сообщить(ош);
		#конецесли
	КонецЕсли;
	
	Возврат рет;
КонецФункции

Функция ЗаписатьЗаявку(Накладная, Ном = Неопределено, ПринятыеЗаявки = Неопределено, ОбработанныеЗаказы = Неопределено)
	НомНакладной = "";

	Если Накладная.Товары.Количество() = 0 Тогда
		Возврат Ложь;	
	КонецЕсли;
	
	рет = ПопыткаЗаписатьДокумент(Накладная, РежимЗаписиДокумента.Запись);
	Если не рет Тогда
		Возврат рет;
	КонецЕсли;
	
	ЗаписатьВЛог("Док записан " + Накладная.Номер + " " + Накладная.Дата);
	Если ном = Неопределено Тогда
		Возврат рет;
	КонецЕсли;
	
	Если НомНакладной = "" Тогда
		НомНакладной = Накладная.Номер;
	КонецЕсли;			
	
	Если НомНакладной <> "" Тогда
		ОбработанныеЗаказы.Добавить();
		ОбработанныеЗаказы.CREATED = Ном;
		ОбработанныеЗаказы.Записать();

		ПринятыеЗаявки.Добавить(Ном, НомНакладной);
	КонецЕсли;
	
	ПопыткаЗаписатьДокумент(Накладная, РежимЗаписиДокумента.Проведение);
	
	типДок = Строка(ТипЗнч(Накладная));

	Сообщить(типДок + " № " + Накладная.Номер + 
			" от " + Накладная.Дата + 
			" контрагент " + Накладная.Контрагент.Наименование);
			
	Возврат Истина;
КонецФункции

Процедура ДобавитьТоварВНакладную(Накладная, номенклатура, кол_во, цена)
	товар = Накладная.Товары.Добавить();
	товар.Номенклатура = номенклатура.Ссылка;
	товар.Склад = Накладная.Склад;

	ОбработкаТабличныхЧастей.ЗаполнитьЕдиницуЦенуПродажиТабЧасти(товар, Накладная, Накладная.ВалютаДокумента);

	коэф = товар.ЕдиницаИзмерения.Коэффициент;
	Если коэф = 0 Тогда
		коэф = 1;
	КонецЕсли;
	товар.Количество = кол_во / коэф;
	товар.Коэффициент = коэф;
	
	Если товар.Цена <> цена Тогда
		Если товар.Цена <> 0 Тогда
			товар.ПроцентСкидкиНаценки = 100 - цена / товар.Цена * 100;
		КонецЕсли;
		//товар.Цена = цена;
	КонецЕсли;
	

	ОбработкаТабличныхЧастей.ЗаполнитьСтавкуНДСТабЧасти(товар, Накладная);
	ОбработкаТабличныхЧастей.РассчитатьСуммуТабЧасти(товар, Накладная);
	ОбработкаТабличныхЧастей.РассчитатьСуммуНДСТабЧасти(товар, Накладная);
КонецПроцедуры

Функция ОстатокТовара(номенклатура, Накладная)
//	рет = 0;
////	отбор = Новый Структура("Организация,Номенклатура,Склад", Накладная.Организация, номенклатура, Накладная.Склад);
//	отбор = Новый Структура("Номенклатура,Склад", номенклатура, Накладная.Склад);
//	кол_во = РегистрыНакопления.ТоварыНаСкладах.Остатки(ТекущаяДата(), отбор);
//	Если кол_во.Количество() > 0 Тогда
//		рет = кол_во[0].Количество;
//	КонецЕсли;
//	
//	Возврат рет;

	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ
	               |	ТоварыНаСкладахОстатки.КоличествоОстаток
	               |ИЗ
	               |	РегистрНакопления.ТоварыНаСкладах.Остатки КАК ТоварыНаСкладахОстатки
	               |ГДЕ
	               |	ТоварыНаСкладахОстатки.Склад = &Склад
	               |	И ТоварыНаСкладахОстатки.Номенклатура = &Номенклатура";
				   
	Запрос.Параметры.Вставить("Номенклатура", номенклатура);
	Запрос.Параметры.Вставить("Склад", Накладная.Склад);
	
	рет1 = 0;
	выб = Запрос.Выполнить().Выбрать();
	Если выб.Следующий() Тогда
		рет1 = выб.КоличествоОстаток;	
	КонецЕсли;

	возврат рет1;
КонецФункции

Функция СкопироватьДокумент(Накладная, Склад)
	рет = Документы.РеализацияТоваровУслуг.СоздатьДокумент();
	
	ЗаполнитьЗначенияСвойств(рет, Накладная, ,"Товары");
	рет.Склад = Склад;
	
	Возврат рет;
КонецФункции

Процедура ПриемЗаявок() Экспорт
	Перем Накладная1, Накладная2, агент;
	
		//ооо
				//ЗаписатьВЛог("логлог");

		//ооо_
	Рез = НайтиФайлы(ПапкаОбмена, "ORD*.DBF");
		
	СчНакл = 0;
	
	Для Каждого файл из Рез Цикл
		имяФайла = файл.Имя;
		разделитель = Найти(ВРег(имяФайла), ".DBF");

		КодСотр = Сред(имяФайла, 4, разделитель-4);
		Агент = НайтиАгента(КодСотр);
		
		можетМенятьЦену = ИзменятьТипЦены;
		
		временныйФайл = ПапкаОбмена + "\TORD.DBF";
		ПереместитьФайл(файл.ПолноеИмя, временныйФайл);
		
		Заказ = Новый XBase(временныйФайл);
		Заказ.Кодировка = КодировкаXBase.OEM;
		ОбработанныеЗаказы = ФайлОбработанныхЗаказов(КодСотр);
		
		ЗаписатьЗаказ = 0;
		ПринятыеЗаявки = ЗагрузитьПринятыеДокументы(КодСотр, Истина);
		Ном = "";
		//ооо
				//ЗаписатьВЛог("логлог");

		//ооо_
		Заказ.Первая();		
		Пока Заказ.ВКонце() = 0 Цикл
			//
			// Проверим признак начала заявки
			//
			Если Заказ.ORDFLAG <> 0 Тогда
				//
				// началась новая заявка, если надо записать заявка, запишем ее.
				//
				Если ЗаписатьЗаказ = 1 Тогда
					Если ЗаписатьЗаявку(Накладная1, Ном, ПринятыеЗаявки, ОбработанныеЗаказы) Тогда
						Ном = Неопределено
					КонецЕсли;
					Если Накладная2 <> Неопределено Тогда
						ЗаписатьЗаявку(Накладная2, Ном, ПринятыеЗаявки, ОбработанныеЗаказы);
						Накладная2 = Неопределено;
					КонецЕсли;
					ЗаписатьЗаказ = 0;
				КонецЕсли;
				
				//
				// Проверим, не принимали ли мы уже эту заявку. Может быть передали одну заявку дважды
				//
				пз = ПринятыеЗаявки.НайтиПоЗначению(СокрЛП(Заказ.CREATED));
				Если пз <> Неопределено Тогда
					//
					// пропускаем этот заказ
					//
					ЗаписатьЗаказ = 0;
					Заказ.Следующая();
					Сообщить("Повторная заявка - накладная № " + пз.Представление);
					Продолжить;
				КонецЕсли;
				
				//
				// создаем новую заявку
				//
				Накладная1 = СоздатьЗаказ(Заказ, Агент, можетМенятьЦену);
				Накладная2 = СоздатьЗаказ(Заказ, Агент, можетМенятьЦену);
				Если Накладная1.Склад = СкладМирМасел Тогда
					Накладная2.Склад = СкладОстатков;
					Накладная2.Организация = ОрганиацияЛукойл;
				Иначе
					Накладная2.Склад = СкладМирМасел;
				КонецЕсли;
				
				//ЗаписатьВЛог("Док '" + Накладная1.Контрагент + "' склад '" + Накладная1.Склад + "' ММ=" + ?(Накладная1.Склад <> СкладМирМасел, "0", "1"));
				//Если Накладная1.Склад <> СкладМирМасел Тогда
				//	Накладная2 = СоздатьЗаказ(Заказ, Агент, можетМенятьЦену);
				//	Накладная2.Склад = СкладМирМасел;
				//Иначе	
				//	Накладная2 = Неопределено;	
				//КонецЕсли;
				ЗаписатьЗаказ = 1;
				Ном = СокрЛП(Заказ.CREATED);
				СчНакл = СчНакл + 1;
			КонецЕсли;
			
			Если ЗаписатьЗаказ = 0 Тогда
				Заказ.Следующая();
				Продолжить;
			КонецЕсли;
			
			Накладная = Неопределено;
			номенклатура = Справочники.Номенклатура.НайтиПоКоду(Заказ.ID_I);
			вЗаказе = Заказ.QTY;
			кол_во = ОстатокТовара(номенклатура, Накладная1);
			ЗаписатьВЛог(" товар '" + номенклатура + "' заказ " + вЗаказе + " на складе " + кол_во);
			Если кол_во > 0 Тогда
				Если кол_во > вЗаказе Тогда
					кол_во = вЗаказе;
				КонецЕсли;
				ДобавитьТоварВНакладную(Накладная1, номенклатура, кол_во, Заказ.COST);
				вЗаказе = вЗаказе - кол_во;
			КонецЕсли;
			
			Если вЗаказе > 0 Тогда
				кол_во = ОстатокТовара(номенклатура, Накладная2);
				// Весь остаток выгружаем на второй склад
				//кол_во = вЗаказе;
				Если кол_во > 0 Тогда
					Если кол_во > вЗаказе Тогда
						кол_во = вЗаказе;
					КонецЕсли;
					ДобавитьТоварВНакладную(Накладная2, номенклатура, кол_во, Заказ.COST);
				КонецЕсли;
			КонецЕсли;
			
			//Если вЗаказе > 0 И Накладная1.Склад <> СкладМирМасел Тогда
			//	ЗаписатьВЛог(" во второю накладную " + вЗаказе);
			//	Если Накладная2 = Неопределено Тогда
			//		Накладная2 = СкопироватьДокумент(Накладная1, СкладМирМасел);
			//	КонецЕсли;
			//	//кол_во = ОстатокТовара(номенклатура, Накладная2);
			//	// Весь остаток выгружаем на второй склад
			//	кол_во = вЗаказе;
			//	Если кол_во > 0 Тогда
			//		Если кол_во > вЗаказе Тогда
			//			кол_во = вЗаказе;
			//		КонецЕсли;
			//		ДобавитьТоварВНакладную(Накладная2, номенклатура, кол_во, Заказ.COST);
			//	КонецЕсли;
			//КонецЕсли;
			
			Заказ.Следующая();
		КонецЦикла;
		
		Если ЗаписатьЗаказ = 1 Тогда
			ЗаписатьЗаявку(Накладная1, Ном, ПринятыеЗаявки, ОбработанныеЗаказы);
			Если Накладная2 <> Неопределено Тогда
				ЗаписатьЗаявку(Накладная2, Ном, ПринятыеЗаявки, ОбработанныеЗаказы);
			КонецЕсли;
		КонецЕсли;
		
		СохранитьПринятыеДокументы(КодСотр, ПринятыеЗаявки);
		Заказ.ЗакрытьФайл();
		ЗакрытьФайлОбработанныхЗаявок(ОбработанныеЗаказы, КодСотр);
		
		УдалитьФайлы(временныйФайл);
	КонецЦикла;
	
	Сообщить("Всего заявок " + СчНакл);
КонецПроцедуры

Процедура ПриОткрытии()
	Орг = УправлениеПользователями.ПолучитьЗначениеПоУмолчанию(глЗначениеПеременной("глТекущийПользователь"), "ОсновнаяОрганизация");
	Если ПараметрЗапуска = "ПринятьЗаявки" Тогда
		ПриемЗаявок();
		ЗавершитьРаботуСистемы(Ложь);
	ИначеЕсли ПараметрЗапуска = "ВыгрузитьДолги" Тогда
		ВыгрузкаДолгов();
		ЗавершитьРаботуСистемы(Ложь);
	ИначеЕсли ПараметрЗапуска = "ВыгрузитьДневныеНакладные" Тогда
		ВыгрузкаДневныхНакладных();	
		ЗавершитьРаботуСистемы(Ложь);
	ИначеЕсли ПараметрЗапуска = "ВыгрузитьДанные" Тогда
		ВыгрузкаОстатков();
		ЗавершитьРаботуСистемы(Ложь);
	КонецЕсли;
КонецПроцедуры
