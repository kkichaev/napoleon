Функция Версия() Экспорт
	Возврат "3.0.5";
КонецФункции

Функция Информация() Экспорт
	Возврат "Обмен информацией с AceTeam для УНФ 3";
КонецФункции

Функция СведенияОВнешнейОбработке() Экспорт
	
    ПараметрыРегистрации = ДополнительныеОтчетыИОбработки.СведенияОВнешнейОбработке();

	ПараметрыРегистрации.Вид = ДополнительныеОтчетыИОбработкиКлиентСервер.ВидОбработкиДополнительнаяОбработка();
    ПараметрыРегистрации.Наименование = "Обмен данными с АСМТ Наполеон";
    ПараметрыРегистрации.БезопасныйРежим = Ложь;
    ПараметрыРегистрации.Версия = Версия();    
    ПараметрыРегистрации.Информация = Информация();
	
    ДобавитьКоманду(ПараметрыРегистрации.Команды, "Выгрузка основных данных", "ВыгрузкаОсновныхДанных", 
		ДополнительныеОтчетыИОбработкиКлиентСервер.ТипКомандыВызовСерверногоМетода());
    
    ДобавитьКоманду(ПараметрыРегистрации.Команды, "Выгрузка долгов", "ВыгрузкаДолгов", 
		ДополнительныеОтчетыИОбработкиКлиентСервер.ТипКомандыВызовСерверногоМетода());

    ДобавитьКоманду(ПараметрыРегистрации.Команды, "Загрузка документов", "ЗагрузкаДокументов", 
		ДополнительныеОтчетыИОбработкиКлиентСервер.ТипКомандыВызовСерверногоМетода());
			
    ДобавитьКоманду(ПараметрыРегистрации.Команды, "Настройка выгрузки", "Настройка", 
		ДополнительныеОтчетыИОбработкиКлиентСервер.ТипКомандыОткрытиеФормы());

		Возврат ПараметрыРегистрации;
КонецФункции

Процедура ДобавитьКоманду(ТаблицаКоманд, Представление, Идентификатор, Использование, ПоказыватьОповещение = Ложь, Модификатор = "")
  НоваяКоманда = ТаблицаКоманд.Добавить();
  НоваяКоманда.Представление = Представление;
  НоваяКоманда.Идентификатор = Идентификатор;
  НоваяКоманда.Использование = Использование;
  НоваяКоманда.ПоказыватьОповещение = ПоказыватьОповещение;
  НоваяКоманда.Модификатор = Модификатор;

КонецПроцедуры

Процедура ВыполнитьФоновоеЗадание(ИдентификаторКоманды, Хранилище) Экспорт
	ПараметрыВыполнения = Новый Структура("РезультатВыполнения", Новый Структура);
	ВыполнитьКоманду(ИдентификаторКоманды, ПараметрыВыполнения);
	Если Хранилище <> Неопределено Тогда
		ПоместитьВоВременноеХранилище(ПараметрыВыполнения.РезультатВыполнения, Хранилище);
	КонецЕсли;
КонецПроцедуры

Процедура СообщитьИнт(Сообщение, ОбъектДанных)
	ЗаписьЖурналаРегистрации("ВыгрузкаНаполеон", УровеньЖурналаРегистрации.Информация, 
		Неопределено, ОбъектДанных, Сообщение);
КонецПроцедуры

Процедура ВыполнитьКоманду(ИдентификаторКоманды, ПараметрыВыполнения = Неопределено) Экспорт
	ЗагрузитьНастройки();
	Если ИдентификаторКоманды = "ВыгрузкаОсновныхДанных" Тогда
		СообщитьИнт("ПапкаОбмена", КодСервера);
		ВыгрузкаОснДанных();
	ИначеЕсли ИдентификаторКоманды = "ВыгрузкаДолгов" Тогда
		ВыгрузкаДолгов();
	ИначеЕсли ИдентификаторКоманды = "ЗагрузкаДокументов" Тогда
		ПриемДокументов();
	КонецЕсли;
КонецПроцедуры

#Область HTTP

Функция ПодключитьсяКСерверу()
	//Возврат Новый HTTPСоединение("localhost");
	ssl = Новый ЗащищенноеСоединениеOpenSSL(
	            Новый СертификатКлиентаWindows(),
	            Новый СертификатыУдостоверяющихЦентровWindows());	
	Возврат Новый HTTPСоединение("napmobile.ru",,,,,,ssl);
КонецФункции

Функция СжатьДанные(данные)
	имяФайла = КаталогВременныхФайлов() + "data";
	тд = Новый ТекстовыйДокумент();
	тд.ДобавитьСтроку(данные);
	тд.Записать(имяФайла, КодировкаТекста.UTF8);
	
	поток = Новый ПотокВПамяти();	
	зип = Новый ЗаписьZipФайла(поток,,,МетодСжатияZIP.Сжатие);
	зип.Добавить(имяФайла);
	зип.Записать();
	
	поток.Перейти(0, ПозицияВПотоке.Начало);
	читать = Новый ЧтениеДанных(поток);
	читать.Пропустить(14);
	crc = читать.ПрочитатьЦелое32();
	размерСжатый = читать.ПрочитатьЦелое32();
	размер = читать.ПрочитатьЦелое32();
	
	// fileNameLength + extraLentgh +fileName+extra = 8 for data filename
	читать.Пропустить(8);
	тело = читать.Прочитать(размерСжатый).ПолучитьДвоичныеДанные();
	
	читать.Закрыть();
	поток.Закрыть();
	
	зппоток = Новый ПотокВПамяти();
	запись = Новый ЗаписьДанных(зппоток);
	
	// GZip Header for data fileName
	запись.Записать(Base64Значение("H4sICAAAAAAEAGRhdGEA"));
	запись.Записать(тело);
	запись.ЗаписатьЦелое32(crc);
	запись.ЗаписатьЦелое32(размер);
	запись.Закрыть();

	Возврат зппоток.ЗакрытьИПолучитьДвоичныеДанные();	
КонецФункции

Функция ИзвлечьДанные(ответ, ошибки, объектДанных = Неопределено, данные = Неопределено)
	рет = Ложь;
	стр = ответ.ПолучитьТелоКакСтроку();
	Попытка
		чтение = Новый ЧтениеJSON;
		чтение.УстановитьСтроку(стр);
		
		рез = ПрочитатьJSON(чтение);
		Для каждого эл из рез Цикл
			имяОбъекта = "";
			Если Не эл.Свойство("name", имяОбъекта) Тогда
				Продолжить;
			КонецЕсли;
			
			элементы = Новый Массив;
			эл.Свойство("data", элементы);
			
			Если имяОбъекта = "ServerAnswer" Тогда
				рет = Истина;
				Для каждого дэл из элементы Цикл
					результат = 0;
					дэл.Свойство("response", результат);
					Если результат = 0 Тогда
						рет = Ложь;
						сообщение = "";
						Если дэл.Свойство("message", сообщение) Тогда
							ошибки.Добавить(сообщение);
						КонецЕсли;
					КонецЕсли;
				КонецЦикла;
			ИначеЕсли имяОбъекта = объектДанных Тогда
				Для каждого дэл из элементы Цикл
					данные.Добавить(дэл);
				КонецЦикла;
			КонецЕсли;
		КонецЦикла;
	Исключение
		ошибки.Добавить(стр);
	КонецПопытки;	
	Возврат рет;
КонецФункции

Функция ОбработкаОтветаСервера(ответ, ошибки, объектДанных = Неопределено, данные = Неопределено)
	код = ответ.КодСостояния;
	Если код < 200 или код > 299 Тогда
		Если код = 413 Тогда
			стр = "Превышен допустимый размер загружаемого файла";	
			ошибки.Добавить(стр);
		Иначе
			ИзвлечьДанные(ответ, ошибки);
		КонецЕсли;
		Возврат ложь;
	КонецЕсли;
	
	Возврат ИзвлечьДанные(ответ, ошибки, объектДанных, данные);
КонецФункции

Функция ОтправитьДанные(данные, метод="POST", сообщения=Неопределено)
    Заголовки = Новый Соответствие;
    Заголовки["Content-Type"] = "application/json";
    Заголовки["Authorization"] = "Bearer " + КодСервера;
	
	тело = СформироватьТелоЗапроса(данные);
    Заголовки["Content-Encoding"] = "gzip";
	тело = СжатьДанные(тело);
	
	Соединение = ПодключитьсяКСерверу();
    Запрос = Новый HTTPЗапрос("/grs/object", Заголовки);
	Запрос.УстановитьТелоИзДвоичныхДанных(тело);
	
	ответ = Соединение.ВызватьHTTPМетод(метод, Запрос);
	
	Если сообщения = Неопределено Тогда
		сообщения = Новый Массив;
	КонецЕсли;
	
	рез = ОбработкаОтветаСервера(ответ, сообщения);
	Возврат рез;
КонецФункции

Функция ЗаменитьНаСервере(данные, названиеОбъекта, фильтрДляУдаления = "", сообщения = Неопределено)
	пакет = Новый Массив();
	пакет.Добавить(СформироватьОбъектДляОтправки(названиеОбъекта
		,данные
		,фильтрДляУдаления	));
	Возврат ОтправитьДанные(пакет, "PUT", сообщения);
КонецФункции

Функция ДобавитьНаСервер(данные, названиеОбъекта = Неопределено, сообщения = Неопределено)
	пакет = Новый Массив();
	Если названиеОбъекта = Неопределено Тогда
		Для Каждого эл из данные Цикл
			пакет.Добавить(СформироватьОбъектДляОтправки(эл.Тип, эл.Данные));			
		КонецЦикла;
	Иначе
		пакет.Добавить(СформироватьОбъектДляОтправки(названиеОбъекта, данные));
	КонецЕсли;
	Возврат ОтправитьДанные(пакет, "POST", сообщения);
КонецФункции

Функция ПолучитьДанные(названиеОбъекта, фильтр="")
    Заголовки = Новый Соответствие;
    Заголовки["Content-Type"] = "application/json";
    Заголовки["Authorization"] = "Bearer " + КодСервера;
	
	п_фильтр = "";
	Если фильтр <> "" Тогда
		п_фильтр = "?" + КодироватьСтроку(фильтр, СпособКодированияСтроки.КодировкаURL, "utf-8");
	КонецЕсли;
	url = "/grs/object/" + названиеОбъекта + п_фильтр;
	
    Запрос = Новый HTTPЗапрос(url, Заголовки);
	Соединение = ПодключитьсяКСерверу();	
	рез = Соединение.ВызватьHTTPМетод("GET",Запрос);
	
	Если рез.КодСостояния < 300 Тогда
		объекты = JSONвОбъекты(рез.ПолучитьТелоКакПоток());
		Для Каждого объект из Объекты Цикл
			имя = "";
			Если объект.Свойство("name", имя) И имя = названиеОбъекта Тогда
				Возврат объект["data"];
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Возврат Новый Массив;
КонецФункции

Функция ПередатьФото(фотоДанные, Товар, сообщения)
	кодТовара = КодОбъекта(Товар);
	
	кодыТоваров = Новый Массив;
	кодыТоваров.Добавить(Новый Структура("id", кодТовара));
	
	имяФото = "price/" + СтрЗаменить(Строка(Товар.УникальныйИдентификатор()), "-", "");
	тело = СформироватьТелоЗапроса(Новый Структура("items,name", кодыТоваров, имяФото));
    Заголовки = Новый Соответствие;
    Заголовки["Content-Type"] = "application/json";
    Заголовки["Authorization"] = "Bearer " + КодСервера;
	
	Соединение = ПодключитьсяКСерверу();
    Запрос = Новый HTTPЗапрос("/grs/file/PricePhoto", Заголовки);
	Запрос.УстановитьТелоИзСтроки(тело, КодировкаТекста.UTF8);
	
	рез = Соединение.ВызватьHTTPМетод("POST", Запрос);
	код = рез.КодСостояния;
	Если код < 200 или код > 299 Тогда
		ОбработкаОтветаСервера(рез, сообщения);
		Возврат Ложь;
	КонецЕсли;
	photoUrl = "/grs" + рез.ПолучитьТелоКакСтроку();
	
	Заголовки = Новый Соответствие;
    Заголовки["Authorization"] = "Bearer " + КодСервера;
    Запрос = Новый HTTPЗапрос(photoUrl, Заголовки);
	Запрос.УстановитьТелоИзДвоичныхДанных(фотоДанные);
	рез = Соединение.ВызватьHTTPМетод("POST", Запрос);
	
	Возврат ОбработкаОтветаСервера(рез, сообщения);
КонецФункции

#КонецОбласти

#Область JSONSerialization

Функция СформироватьОбъектДляОтправки(наименование, данные, фильтрДляУдаления = Неопределено)
	Если ТипЗнч(данные) = Тип("Структура") Тогда
		тд = Новый Массив;
		тд.Добавить(данные);
		данные = тд;
	КонецЕсли;
	Возврат ?(фильтрДляУдаления = Неопределено
		,Новый Структура("name,data", наименование, данные)
		,Новый Структура("name,data,where", наименование, данные, фильтрДляУдаления)
	);
КонецФункции

Функция СформироватьТелоЗапроса(данные)
	запись = Новый ЗаписьJSON();

	параметры = Новый ПараметрыЗаписиJSON();
	запись.УстановитьСтроку(параметры);
	
	ЗаписатьJSON(запись, данные);
	
	Возврат запись.Закрыть();
КонецФункции

Функция ДатаВСтроку(датаЗнач)
	Возврат Формат(датаЗнач, "ДФ=ггггММддЧЧммсс");
КонецФункции

Функция JSONвОбъекты(поток)
	json = Новый ЧтениеJSON;
	json.ОткрытьПоток(поток, КодировкаТекста.UTF8);
	Возврат ПрочитатьJSON(json, Ложь);
КонецФункции

#КонецОбласти


Функция КодОбъекта(эл)
	тз = ТипЗнч(эл);
	Если тз = Тип("СправочникСсылка.Пользователи")
		или тз = Тип("СправочникСсылка.ЗначенияСвойствОбъектов")
	Тогда
		Возврат Строка(эл.УникальныйИдентификатор());
	КонецЕсли;
	
	Возврат СокрЛП(эл.Код);
КонецФункции

Функция ПолучитьОбъект(эл, код)
	тз = ТипЗнч(эл);
	Если тз = Тип("СправочникМенеджер.Пользователи")
		или тз = Тип("СправочникМенеджер.ЗначенияСвойствОбъектов")
	Тогда
		уид = Новый УникальныйИдентификатор(СокрЛП(код));
		Возврат эл.ПолучитьСсылку(уид);
	КонецЕсли;
	
	Возврат эл.НайтиПоКоду(СокрЛП(код));
КонецФункции

Функция ВыгрузитьКонтакты(Партнер)
	СписокКонтактов = Новый Массив;
	Для каждого стр из Партнер.КонтактнаяИнформация Цикл
		Если стр.Тип = Перечисления.ТипыКонтактнойИнформации.Телефон Тогда
			тел = ?(СтрДлина(стр.НомерТелефона) = 0, стр.Представление, стр.НомерТелефона);
			Если Не ПустаяСтрока(тел) Тогда
				СписокКонтактов.Добавить(Новый Структура("name,phone"
					,партнер.Наименование
					,тел
				));
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Возврат СписокКонтактов;
КонецФункции

Функция АдресКонтрагента(Контрагент)
	Возврат УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(Контрагент
	, Справочники.ВидыКонтактнойИнформации.ФактАдресКонтрагента
	, ТекущаяДата());
КонецФункции

Процедура ЗаписатьКонтакты(Контакты, СписокКонтактов, код)
	Если СписокКонтактов = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого стр Из СписокКонтактов Цикл
		Контакты.Добавить();
		
		Контакты.ID = код;
		Контакты.FIO = стр.Представление;
		Контакты.PHONE = стр.Значение;
		
		Контакты.Записать();
	КонецЦикла;
КонецПроцедуры

Процедура ВыгрузкаКонтрагентов(ВидыЦен)
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Контрагенты.Ссылка КАК Ссылка,
	|	ОсновныеДоговорыКонтрагента.Договор КАК Договор,
	|	Контрагенты.Ответственный КАК Агент,
	|	ОсновныеДоговорыКонтрагента.Организация КАК Организация,
	|	ОсновныеДоговорыКонтрагента.Договор.ВидЦен КАК ВидЦены
	|ИЗ
	|	Справочник.Контрагенты КАК Контрагенты
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ОсновныеДоговорыКонтрагента КАК ОсновныеДоговорыКонтрагента
	|		ПО (ОсновныеДоговорыКонтрагента.Контрагент = Контрагенты.Ссылка)
	|ГДЕ
	|	Контрагенты.Ответственный <> ЗНАЧЕНИЕ(Справочник.Сотрудники.ПустаяСсылка)
	|	И ОсновныеДоговорыКонтрагента.Организация = &Организация
	|
	|УПОРЯДОЧИТЬ ПО
	|	Агент";
	Запрос.УстановитьПараметр("Организация", Организация);
	выгружен = Новый Массив;
	
	агенты = Новый Массив; // сервер.New("AgentUpdate");
	орг = Новый Массив; //сервер.New("Org");
	данныеАгента = Новый Массив; //сервер.New("AgentData");
	
	текАгент = Неопределено;

	выб = Запрос.Выполнить().Выбрать();
	Пока выб.Следующий() Цикл
		Если текАгент <> выб.Агент Тогда

			текАгент = выб.Агент;
			
			агент = Новый Структура("id,name"
				,КодОбъекта(текАгент)
				,текАгент.Наименование
			);
			агенты.Добавить(агент);
			
		КонецЕсли;
		
		тц = ВидыЦен[0];
		Если ЗначениеЗаполнено(выб.ВидЦены) Тогда
			тц = выб.ВидЦены;
			тст = ВидыЦен.Найти(выб.ВидЦены);
			Если тст = Неопределено Тогда
				ВидыЦен.Добавить(выб.ВидЦены);
			КонецЕсли;
		КонецЕсли;
		
		код = КодОбъекта(выб.Ссылка);			
		контакты = ВыгрузитьКонтакты(выб.Ссылка);
		контрагент = Новый Структура("id,name,address,prcType,contacts"
			,код
			,выб.Ссылка.Наименование
			,АдресКонтрагента(выб.Ссылка)
			,КодОбъекта(тц)
			,контакты
		);
		орг.Добавить(контрагент);

		
		// связываем контрагента и агента
		данные = Новый Структура("userid,id,type"
			,КодОбъекта(текАгент)
			,код
			,"Org"
		);
		данныеАгента.Добавить(данные);
	КонецЦикла;

	ДобавитьНаСервер(агенты, "Agents"); 
	ЗаменитьНаСервере(орг, "Org");
	ЗаменитьНаСервере(данныеАгента, "AgentData", """type""='Org' or ""type""='PriceType'");
КонецПроцедуры
                                                 		
Процедура ВыгрузитьПапкиТоваров()
	папки = Новый Массив; //сервер.New("Folder");
	папкиПоля = Новый Соответствие();
	папкиПоля.Вставить("fid","Код");
	папкиПоля.Вставить("name","Наименование");
	папкиПоля.Вставить("parent","РодительКод");
	
	папки.Добавить(Новый Структура("fid,parent,name"
		,"0"
		,""
		,"Товары"
	));

	папкиТоваров = Справочники.Номенклатура.ВыбратьИерархически();
	Пока папкиТоваров.Следующий() Цикл
		Если папкиТоваров.ПометкаУдаления или (папкиТоваров.ЭтоГруппа = Ложь) Тогда
			Продолжить;
		КонецЕсли;
		
		папки.Добавить(Новый Структура("fid,parent,name"
			,КодОбъекта(папкиТоваров)
			,?(папкиТоваров.Родитель.Пустая(), "", КодОбъекта(папкиТоваров.Родитель))
			,папкиТоваров.Наименование
		));
	КонецЦикла;
	
	ЗаменитьНаСервере(папки,"Folder");
КонецПроцедуры

Функция ПолучитьДопРеквизит(Наименование)
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ДополнительныеРеквизитыИСведения.Ссылка КАК Ссылка,
	|	ДополнительныеРеквизитыИСведения.Имя КАК Имя
	|ИЗ
	|	ПланВидовХарактеристик.ДополнительныеРеквизитыИСведения КАК ДополнительныеРеквизитыИСведения
	|ГДЕ
	|	ДополнительныеРеквизитыИСведения.Наименование = &Наименование";
	
	Запрос.УстановитьПараметр("Наименование", Наименование);
	выб = Запрос.Выполнить().Выбрать();
	Если выб.Следующий() Тогда
		Возврат выб.Ссылка;
	КонецЕсли;
	Возврат ПланыВидовХарактеристик.ДополнительныеРеквизитыИСведения.ПустаяСсылка();
КонецФункции

Функция ВыгрузитьУпаковки()
	
	типУпак = ПолучитьДопРеквизит("тип упаковки");
	колУпак = ПолучитьДопРеквизит("Количество в упаковке");
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	НоменклатураДополнительныеРеквизиты.Ссылка КАК Номенклатура,
	|	НоменклатураДополнительныеРеквизиты.Значение КАК Упаковка,
	|	КоличествоВУпаковке.Значение КАК Количество
	|ИЗ
	|	Справочник.Номенклатура.ДополнительныеРеквизиты КАК НоменклатураДополнительныеРеквизиты
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
	|			НоменклатураДополнительныеРеквизиты.Ссылка КАК Ссылка,
	|			НоменклатураДополнительныеРеквизиты.Значение КАК Значение
	|		ИЗ
	|			Справочник.Номенклатура.ДополнительныеРеквизиты КАК НоменклатураДополнительныеРеквизиты
	|		ГДЕ
	|			НоменклатураДополнительныеРеквизиты.Свойство = &КоличествоВУпаковке) КАК КоличествоВУпаковке
	|		ПО (КоличествоВУпаковке.Ссылка = НоменклатураДополнительныеРеквизиты.Ссылка)
	|ГДЕ
	|	НоменклатураДополнительныеРеквизиты.Свойство = &ТипУпаковки";
	
	Запрос.УстановитьПараметр("КоличествоВУпаковке", колУпак);
	Запрос.УстановитьПараметр("ТипУпаковки", типУпак);
	
	Возврат Запрос.Выполнить().Выгрузить();	
КонецФункции

Процедура ВыгрузитьТовары(ВидыЦен)
	мСклады = Новый Массив; //сервер.New("Stores");	
	мСклады.Добавить(Новый Структура( "id,name"
		,КодОбъекта(Склад)
		,Склад.Наименование
	));
	ЗаменитьНаСервере(мСклады, "Stores");
	
	упакТбл = ВыгрузитьУпаковки();

	//Запрос = Новый Запрос;
	//Запрос.Текст = 
	//	"ВЫБРАТЬ
	//	|	ЕдиницыИзмерения.Владелец КАК Номенклатура,
	//	|	ЕдиницыИзмерения.Коэффициент КАК Коэффициент,
	//	|	ЕдиницыИзмерения.Ссылка КАК Ссылка,
	//	|	ЕдиницыИзмерения.Код КАК Код
	//	|ИЗ
	//	|	Справочник.ЕдиницыИзмерения КАК ЕдиницыИзмерения
	//	|ГДЕ
	//	|	ЕдиницыИзмерения.Коэффициент <> 1";
	//едИзм = Запрос.Выполнить().Выгрузить();
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	Цены.Номенклатура КАК Номенклатура,
		|	Цены.Цена КАК Цена,
		|	ЕСТЬNULL(Остатки.КоличествоОстаток, 0) КАК Остаток,
		|	Цены.ВидЦен КАК ВидЦены,
		|	Цены.Коэффициент КАК Коэффициент,
		|	Цены.КодЕИ КАК КодЕИ
		|ИЗ
		|	(ВЫБРАТЬ
		|		ЦеныНоменклатурыСрезПоследних.Номенклатура КАК Номенклатура,
		|		ЦеныНоменклатурыСрезПоследних.Цена КАК Цена,
		|		ЦеныНоменклатурыСрезПоследних.ВидЦен КАК ВидЦен,
		|		ЕСТЬNULL(ВложенныйЗапрос.Коэффициент, 1) КАК Коэффициент,
		|		ВложенныйЗапрос.ЕдиницаИзмеренияПоКлассификаторуКод КАК КодЕИ
		|	ИЗ
		|		РегистрСведений.ЦеныНоменклатуры.СрезПоследних(, НЕ Номенклатура В ИЕРАРХИИ (&Папки)) КАК ЦеныНоменклатурыСрезПоследних
		|			ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|				Номенклатура.Ссылка КАК Ссылка,
		|				ЕдиницыИзмерения.Коэффициент КАК Коэффициент,
		|				ЕдиницыИзмерения.ЕдиницаИзмеренияПоКлассификатору.Код КАК ЕдиницаИзмеренияПоКлассификаторуКод
		|			ИЗ
		|				Справочник.Номенклатура КАК Номенклатура
		|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ЕдиницыИзмерения КАК ЕдиницыИзмерения
		|					ПО (ЕдиницыИзмерения.Владелец = Номенклатура.Ссылка)
		|						И (ЕдиницыИзмерения.ЕдиницаИзмеренияПоКлассификатору = Номенклатура.ЕдиницаИзмерения)) КАК ВложенныйЗапрос
		|			ПО ЦеныНоменклатурыСрезПоследних.Номенклатура = ВложенныйЗапрос.Ссылка
		|	ГДЕ
		|		ЦеныНоменклатурыСрезПоследних.ВидЦен = &ВидЦен) КАК Цены
		|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			ЗапасыОстатки.Номенклатура КАК Номенклатура,
		|			ЗапасыОстатки.КоличествоОстаток КАК КоличествоОстаток
		|		ИЗ
		|			РегистрНакопления.Запасы.Остатки КАК ЗапасыОстатки
		|		ГДЕ
		|			ЗапасыОстатки.СтруктурнаяЕдиница = &Склад
		|			И ЗапасыОстатки.ЗаказПокупателя = ЗНАЧЕНИЕ(Документ.ЗаказПокупателя.ПустаяСсылка)) КАК Остатки
		|		ПО Цены.Номенклатура = Остатки.Номенклатура
		|
		|УПОРЯДОЧИТЬ ПО
		|	Номенклатура";	
	
	папки = ИсключатьПапкиТоваров.ВыгрузитьКолонку("Папка");
	Запрос.УстановитьПараметр("Склад", Склад);
	Запрос.УстановитьПараметр("ВидЦен", ТипЦеныПрайс);
	Запрос.УстановитьПараметр("Папки", Папки);
		
	товары = Новый Массив; //сервер.New("Price");
	мЦены = Новый Массив;// сервер.New("PriceCost");
	остатки = Новый Массив; //сервер.New("StoreQty"); 
	
	текТовар = Неопределено;

	выб = Запрос.Выполнить().Выбрать();	
	Пока выб.Следующий() Цикл
		Если текТовар <> выб.Номенклатура Тогда
			текТовар = выб.Номенклатура;
			
			Код = КодОбъекта(текТовар);
			
			упак = Новый Массив;			
			упак.Добавить(Новый Структура("id,code,name,inpack"
				,""
				,текТовар.ЕдиницаИзмерения.Код
				,текТовар.ЕдиницаИзмерения.Наименование
				,1
			));
			//естр = едИзм.НайтиСтроки(Новый Структура("Номенклатура", текТовар));
			//Для Каждого тстр из естр Цикл
			//	упак.Добавить(Новый Структура("id,code,name,inpack"
			//		,КодОбъекта(тстр.Ссылка)
			//		,тстр.Код
			//		,тстр.Ссылка.Наименование
			//		,тстр.Коэффициент
			//	));
			//КонецЦикла;
			естр = упакТбл.НайтиСтроки(Новый Структура("Номенклатура", текТовар));
			Для Каждого тстр из естр Цикл
				упак.Добавить(Новый Структура("id,code,name,inpack"
					,КодОбъекта(тстр.Упаковка)
					,тстр.Упаковка.Наименование
					,тстр.Упаковка.Наименование
					,тстр.Количество
				));
			КонецЦикла;

			товары.Добавить(Новый Структура( "id,fid,name,units,weight"
				,Код
				,?(ЗначениеЗаполнено(текТовар.Родитель), КодОбъекта(текТовар.Родитель), "0")
				,текТовар.Наименование
				,упак
				,текТовар.вес
			));
			
			Если выб.Остаток <> 0 Тогда
				остатки.Добавить(Новый Структура( "id,idItem,qty"
					,КодОбъекта(Склад)
					,Код
					,выб.Остаток
				));
			КонецЕсли;
			
		КонецЕсли;
			
		мЦены.Добавить(Новый Структура("id,idItem,cost"
			,КодОбъекта(выб.ВидЦены)
			,Код
			,выб.Цена));
	КонецЦикла;
	
	ЗаменитьНаСервере(остатки, "StoreQty");
	ЗаменитьНаСервере(мЦены, "PriceCost");	
	ЗаменитьНаСервере(товары, "Price");
	
КонецПроцедуры

Процедура ВыгрузитьНастройки(цены)
	
	тц = Новый Массив; //сервер.New("PriceTypes");	
	Для Каждого стр из цены Цикл
		тц.Добавить(Новый Структура("id,name"
			,КодОбъекта(стр.Ссылка)
			,стр.Наименование));
	КонецЦикла;
	
	фирмы = Новый Массив; //сервер.New("Firm");	
	фирмы.Добавить(Новый Структура("id,name"
		,КодОбъекта(Организация)
		,Организация.Наименование));
	
	данные = Новый Массив();
	данные.Добавить(СформироватьОбъектДляОтправки("PriceTypes", тц, ""));
	данные.Добавить(СформироватьОбъектДляОтправки("Firm", фирмы, ""));
	ОтправитьДанные(данные, "PUT");

КонецПроцедуры

Процедура ВыгрузкаОснДанных() Экспорт
	ВидыЦен = Новый Массив;
	Если не ТипЦеныПрайс.Пустая() Тогда
		ВидыЦен.Добавить(ТипЦеныПрайс);
	КонецЕсли;
	
	ВыгрузкаКонтрагентов(ВидыЦен);
	ВыгрузитьПапкиТоваров();
	ВыгрузитьТовары(ВидыЦен);
	ВыгрузитьНастройки(ВидыЦен);

КонецПроцедуры

#Область ЗагрузкаДокументов

Процедура Ожидание(Таймаут)
    ВремяЗавершения = CurrentDate() + Таймаут;
    While CurrentDate() < ВремяЗавершения Do
        Try
            Соединение = New FTPConnection("127.0.0.1", 2987);
        Except
        EndTry;                    
    EndDo;
КонецПроцедуры

Функция ЗаписатьДокументСервер(док, Проводить = Ложь, сообщения)
	рет = Ложь;
	
	сч = 0;
	
	ош = "";
	Пока сч < 3 и рет = Ложь Цикл
		Попытка
			Если Проводить Тогда
				док.Записать(РежимЗаписиДокумента.Проведение);
			Иначе
				док.Записать();
			КонецЕсли;
			рет = Истина;
			Прервать;
		Исключение
			Если ош = "" Тогда
				ош = ОписаниеОшибки();
			КонецЕсли;
			
			Если Проводить Тогда
				Прервать;
			КонецЕсли;
		КонецПопытки;
		
		Ожидание(4);		
		сч = сч + 1;
	КонецЦикла;
	
	Если Не рет Тогда
		Сообщения.Добавить(ош);
	КонецЕсли;
	Возврат рет;
КонецФункции

Функция ЗаписатьЗаявку(Документ, Сообщения)
	Документ.СуммаДокумента = Документ.Запасы.Итог("Сумма");	
	Возврат ЗаписатьДокументСервер(Документ, Ложь, Сообщения);	
КонецФункции

Функция СоздатьЗаявку(ДокИст)
	контрагент = ПолучитьОбъект(Справочники.Контрагенты, ДокИст.id);
	Агент = ПолучитьОбъект(Справочники.Сотрудники, ДокИст.userid);

	Документ = Документы.ЗаказПокупателя.СоздатьДокумент();
	Документ.Дата = ТекущаяДата();
	Документ.ДатаОтгрузки = Дата(ДокИст.date);;
	Документ.ОжидаемаяДатаВручения = ДокИст.DATE;
	Документ.ВидЗаказа = Справочники.ВидыЗаказовПокупателей.Основной;
	Документ.ВидОперации = Перечисления.ВидыОперацийЗаказПокупателя.ЗаказНаПродажу;
	Документ.СостояниеЗаказа = СостояниеЗаказа;
	
	Документ.Ответственный = Агент;
	Документ.Контрагент = контрагент;
	
	Документ.НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.НеОблагаетсяНДС;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ДоговорыКонтрагентов.Ссылка КАК Ссылка
		|ИЗ
		|	Справочник.ДоговорыКонтрагентов КАК ДоговорыКонтрагентов
		|ГДЕ
		|	ДоговорыКонтрагентов.ВидДоговора = &ВидДоговора
		|	И ДоговорыКонтрагентов.Владелец = &Владелец";
	
	Запрос.УстановитьПараметр("ВидДоговора", Перечисления.ВидыДоговоров.СПокупателем);
	Запрос.УстановитьПараметр("Владелец", контрагент);
	
	выб = Запрос.Выполнить().Выбрать();
	Если выб.Следующий() Тогда
		Документ.Договор = выб.Ссылка;
	КонецЕсли;
	
	Документ.Организация = Организация;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 		
		"ВЫБРАТЬ
		|	СистемыНалогообложенияОрганизацийСрезПоследних.СистемаНалогообложения КАК СистемаНалогообложения
		|ИЗ
		|	РегистрСведений.СистемыНалогообложенияОрганизаций.СрезПоследних КАК СистемыНалогообложенияОрганизацийСрезПоследних
		|ГДЕ
		|	СистемыНалогообложенияОрганизацийСрезПоследних.Организация = &Организация";
	
	Запрос.УстановитьПараметр("Организация", Документ.Организация);
	выб = Запрос.Выполнить().Выбрать();
	Если выб.Следующий() Тогда
		Если выб.СистемаНалогообложения = Перечисления.СистемыНалогообложения.Общая Тогда
			Документ.НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.ОблагаетсяНДС;
			Документ.НДСВключатьВСтоимость = Истина;
		Иначе
		КонецЕсли;
	КонецЕсли;
	
	Документ.СтруктурнаяЕдиницаПродажи = Склад;
	Документ.СтруктурнаяЕдиницаРезерв = Склад;
	
	Документ.ХозяйственнаяОперация = Справочники.ХозяйственныеОперации.ЗаказНаПродажу;
	Документ.СпособДоставки = Перечисления.СпособыДоставки.Самовывоз;

	Документ.ВидЦен = ПолучитьОбъект(Справочники.ВидыЦен, ДокИст.prcType);
	Документ.ВалютаДокумента = Документ .Договор.ВалютаРасчетов;
	Документ.Кратность = 1;
	Документ.Курс = 1;
	Документ.Комментарий = ДокИст.remark;
	
	Возврат Документ;
КонецФункции

Процедура ДобавитьТовар(Документ, ДокИст)
	Товар = Документ.Запасы.Добавить();
	
	Номенклатура = ПолучитьОбъект(Справочники.Номенклатура, ДокИст.id);
	
	Товар.Номенклатура = Номенклатура;
	Товар.Количество = ДокИст.qty;
	
	ЗаполнениеОбъектовУНФ.ЗаполнитьСтрокуПоШапке(Товар, Документ, "ДатаОтгрузки", "ПоложениеДатыОтгрузки");		
	ЗаполнениеОбъектовУНФ.ЗаполнитьСтрокуПоШапке(Товар, Документ, "СтруктурнаяЕдиницаРезерв", "ПоложениеСклада");
	
	Если Документ.НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.НеОблагаетсяНДС Тогда
		Товар.СтавкаНДС = УправлениеНебольшойФирмойПовтИсп.ПолучитьСтавкуНДСБезНДС();
	ИначеЕсли ЗначениеЗаполнено(Товар.Номенклатура.ВидСтавкиНДС) Тогда
		Товар.СтавкаНДС = Справочники.СтавкиНДС.СтавкаНДС(Товар.Номенклатура.ВидСтавкиНДС, Документ.Дата);
	Иначе
		Товар.СтавкаНДС = Справочники.СтавкиНДС.СтавкаНДС(Документ.Организация.ВидСтавкиНДСПоУмолчанию, Документ.Дата);
	КонецЕсли;
	
	СтруктураДанныеНоменклатуры = Новый Структура();
	СтруктураДанныеНоменклатуры.Вставить("Номенклатура", Товар.Номенклатура);
	СтруктураДанныеНоменклатуры.Вставить("Коэффициент", 1);
	СтруктураДанныеНоменклатуры.Вставить("Характеристика", Товар.Характеристика);
	СтруктураДанныеНоменклатуры.Вставить("ДатаОбработки", Документ.Дата);
	СтруктураДанныеНоменклатуры.Вставить("Организация", Документ.Организация);
	СтруктураДанныеНоменклатуры.Вставить("НалогообложениеНДС", Документ.НалогообложениеНДС);
	СтруктураДанныеНоменклатуры.Вставить("СуммаВключаетНДС", Документ.СуммаВключаетНДС);
	СтруктураДанныеНоменклатуры.Вставить("ВалютаДокумента", Документ.ВалютаДокумента);
	СтруктураДанныеНоменклатуры.Вставить("ВидЦен", Документ.ВидЦен);
	СтруктураДанныеНоменклатуры.Вставить("СтавкаНДС", Товар.СтавкаНДС);
	
	Товар.Цена = ЦенообразованиеСервер.ПолучитьЦенуНоменклатурыПоВидуЦен(СтруктураДанныеНоменклатуры);
	
	Товар.Сумма = Товар.Цена * Товар.Количество;
	Товар.ЕдиницаИзмерения = Номенклатура.ЕдиницаИзмерения;
	Товар.КлючСвязи = Документ.Запасы.Количество() + 1;
	Товар.ТипНоменклатурыЗапас = Истина;
		
	ЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Товар.Номенклатура, "ТипНоменклатуры, Вес, Объем, ЭтоНабор, СпособРасчетаЦеныНабора");
		
	ВесЕдиницыТовара = ЗначенияРеквизитов.Вес;
	Если ВесЕдиницыТовара <> Неопределено Тогда
		Товар.Вес = ВесЕдиницыТовара * Товар.Количество;
	КонецЕсли;
	ОбъемЕдиницыТовара = ЗначенияРеквизитов.Объем;
	Если ОбъемЕдиницыТовара <> Неопределено Тогда
		Товар.Объем = ОбъемЕдиницыТовара * Товар.Количество;
	КонецЕсли;

	
	СтавкаНДС = УправлениеНебольшойФирмойПовтИсп.ПолучитьЗначениеСтавкиНДС(Товар.СтавкаНДС);
	Товар.СуммаНДС = ?(Документ.СуммаВключаетНДС, 
							  		Товар.Сумма - (Товар.Сумма) / ((СтавкаНДС + 100) / 100),
							  		Товар.Сумма * СтавкаНДС / 100);
	Товар.Всего = Товар.Сумма + ?(Документ.СуммаВключаетНДС, 0, Товар.СуммаНДС);
КонецПроцедуры

Процедура ОтправитьОбработанныеДокументы(обработано)
	Если обработано.Количество() >0 Тогда
		пакет = Новый Массив();
		пакет.Добавить(СформироватьОбъектДляОтправки("DocCommitted", обработано));
		пакет.Добавить(СформироватьОбъектДляОтправки("DocProceeded", обработано));
		ОтправитьДанные(пакет, "POST");
	КонецЕсли;
КонецПроцедуры

Процедура ЗагрузитьЗаявки(Сообщения)	
	обработано = Новый Массив;
	
	заявки = ПолучитьДанные("NewOrders");
	Для каждого о из заявки Цикл
		док = СоздатьЗаявку(о);
		
		Для Каждого товар из о.items Цикл
			ДобавитьТовар(док, товар);
		КонецЦикла;
		
		Если ЗаписатьЗаявку(док, Сообщения) Тогда
			обработано.Добавить(Новый Структура("userid,created,type,number,date"
				,о.userid
				,о.created
				,"Order"
				,док.Номер
				,ДатаВСтроку(док.Дата)
			));
			сообщения.Добавить(Строка(док));
		КонецЕсли;
	КонецЦикла;
	
	ОтправитьОбработанныеДокументы(обработано);
КонецПроцедуры

Функция СоздатьПоступлениеДС(ДокИст, Сумма, Сообщения)
	контрагент = ПолучитьОбъект(Справочники.Контрагенты, ДокИст.id);
	Документ = Документы.ПоступлениеВКассу.СоздатьДокумент();
	
	Документ.Дата = ТекущаяДата();
	договор = Справочники.ДоговорыКонтрагентов.ПустаяСсылка();	
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ОсновныеДоговорыКонтрагента.Организация КАК Организация,
	|	ОсновныеДоговорыКонтрагента.Договор КАК Договор
	|ИЗ
	|	РегистрСведений.ОсновныеДоговорыКонтрагента КАК ОсновныеДоговорыКонтрагента
	|ГДЕ
	|	ОсновныеДоговорыКонтрагента.Контрагент = &Контрагент
	|	И ОсновныеДоговорыКонтрагента.ВидДоговора = ЗНАЧЕНИЕ(Перечисление.ВидыДоговоров.СПокупателем)";
	Запрос.УстановитьПараметр("Контрагент", контрагент);
	
	выб = Запрос.Выполнить().Выбрать();
	Если выб.Следующий() Тогда
		Документ.Организация = выб.Организация;
		договор = выб.Договор;
		Документ.НалогообложениеНДС = НалогиУНФ.НалогообложениеНДС(Документ.Организация, , Документ.Дата);
		Документ.Касса = Документ.Организация.КассаПоУмолчанию;
	КонецЕсли;
	
	Документ.ВидОперации = Перечисления.ВидыОперацийПоступлениеВКассу.ОтПокупателя;
	Документ.Статья = Справочники.СтатьиДвиженияДенежныхСредств.ОплатаОтПокупателей;
	Документ.ПринятоОт = Документ.Контрагент.НаименованиеПолное;
	Документ.ВалютаДенежныхСредств = договор.ВалютаРасчетов;
	
	Документ.Курс = 1;
	Документ.Кратность = 1;
		
	Документ.Комментарий = ДокИст.remark;
	
	Документ.Контрагент = контрагент;
		
	Документ.СуммаДокумента = Сумма;
	Документ.СуммаУчета = Сумма;
	
	Если Документ.НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.ОблагаетсяНДС Тогда
		СтавкаНДСПоУмолчанию = Справочники.СтавкиНДС.СтавкаНДС(Документ.Организация.ВидСтавкиНДСПоУмолчанию, Документ.Дата); 
	ИначеЕсли Документ.НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.НеОблагаетсяНДС Тогда
		СтавкаНДСПоУмолчанию = УправлениеНебольшойФирмойПовтИсп.ПолучитьСтавкуНДСБезНДС();
	Иначе
		СтавкаНДСПоУмолчанию = УправлениеНебольшойФирмойПовтИсп.ПолучитьСтавкуНДСНоль();
	КонецЕсли;
	СтавкаНДСПоУмолчаниюЧисло = УправлениеНебольшойФирмойПовтИсп.ПолучитьЗначениеСтавкиНДС(СтавкаНДСПоУмолчанию);

	рп = Документ.РасшифровкаПлатежа.Добавить();
	рп.Договор = договор;
	рп.СуммаРасчетов = Сумма;
	рп.Курс = Документ.Курс;
	рп.Кратность = Документ.Кратность;
	рп.СуммаПлатежа = Сумма;
	рп.СтавкаНДС = СтавкаНДСПоУмолчанию;
	рп.СуммаНДС = Сумма - Сумма / ((СтавкаНДСПоУмолчаниюЧисло + 100) / 100);
	рп.СуммаНДС = Документ.Контрагент.СтатьяДДСПоУмолчанию;
		
	Если ЗаписатьДокументСервер(Документ, Ложь, Сообщения) Тогда
		Возврат Документ;
	КонецЕсли;
	Возврат Неопределено;
КонецФункции

Процедура РаспределитьСумму(Документ, Сумма)
	коэф = Сумма / Документ.СуммаДокумента;
	сумНДС = Документ.РасшифровкаПлатежа.Итог("СуммаНДС") * коэф;
	
	Документ.СуммаДокумента = Сумма;
	Документ.СуммаУчета = Сумма;
	
	Для сч = 0 По Документ.РасшифровкаПлатежа.Количество() - 1 Цикл
		стр = Документ.РасшифровкаПлатежа[сч];
		Если сч = Документ.РасшифровкаПлатежа.Количество() - 1 Тогда
			стр.СуммаРасчетов = Сумма;
			стр.СуммаПлатежа = Сумма;
			стр.СуммаНДС = сумНДС;
		Иначе
			стрСум = стр.СуммаРасчетов * коэф;
			стрНДС = стр.СуммаНДС * коэф;
			сумНдс = сумНдс - стрНДС;
			Сумма = Сумма - стрСум;
			
			стр.СуммаРасчетов = стрСум;
			стр.СуммаПлатежа = стрСум;
			стр.СуммаНДС = стрНДС;
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

Процедура ЗагрузитьИнкассации(сообщения)
	обработано = Новый Массив;
	
	новыеДок = ПолучитьДанные("NewIncass");
	Для каждого д из новыеДок Цикл
		загрДок = Новый Соответствие;
		
		текСум = д.sum;
		Для Каждого эл из д.items Цикл
			ДокОсн = Документы.РасходнаяНакладная.НайтиПоНомеру(эл.number,  Дата(эл.date));
			
			Если Не ДокОсн.Пустая() Тогда
				Документ = Документы.ПоступлениеВКассу.СоздатьДокумент();
				Документ.Заполнить(ДокОсн);
				Документ.Дата = ТекущаяДата();
				Документ.Комментарий = д.remark;
				Если Документ.СуммаДокумента <> эл.sum Тогда
					РаспределитьСумму(Документ, эл.sum);
				КонецЕсли;
				
				Если ЗаписатьДокументСервер(Документ, Ложь, сообщения) Тогда
					обработано.Добавить(Новый Структура("userid,created,type,number,date"
						,д.userid
						,д.created
						,"Incass"
						,Документ.Номер
						,ДатаВСтроку(Документ.Дата)
					));
					сообщения.Добавить(Строка(Документ));
				КонецЕсли;
			КонецЕсли;

			текСум = текСум - эл.sum;
		КонецЦикла;
		
		Если ТекСум > 0 Тогда
			Документ = СоздатьПоступлениеДС(д, ТекСум, Сообщения);
			Если Документ <> Неопределено Тогда
					обработано.Добавить(Новый Структура("userid,created,type,number,date"
						,д.userid
						,д.created
						,"Incass"
						,Документ.Номер
						,ДатаВСтроку(Документ.Дата)
					));
					сообщения.Добавить(Строка(Документ));
				КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	ОтправитьОбработанныеДокументы(обработано);
КонецПроцедуры

Процедура ПриемДокументов() Экспорт
	сообщения = Новый Массив;
	
	ЗагрузитьЗаявки(сообщения);
	ЗагрузитьИнкассации(сообщения);
	
	Для каждого стр из сообщения Цикл
		Сообщить(стр);
	КонецЦикла;
КонецПроцедуры

#КонецОбласти


Процедура ЗагрузитьНастройки() Экспорт	
	Попытка
		спр = Справочники.ДополнительныеОтчетыИОбработки.НайтиПоРеквизиту("ИмяОбъекта", "ВыгрузкаНаполеон");
		настройки = спр.ХранилищеНастроек.Получить();
		Если настройки = Неопределено Тогда
			настройки = ХранилищеОбщихНастроек.Загрузить("NapoleonExchange","NapoleonExchange");
		КонецЕсли;	
		Если настройки <> Неопределено Тогда
			Для каждого стр из Настройки Цикл
				Если стр.Ключ = "ИсключатьПапкиТоваров" Тогда
					Для каждого тст из стр.Значение Цикл
						встр = ИсключатьПапкиТоваров.Добавить();
						встр.Папка = тст;
					КонецЦикла;
				Иначе
					Попытка
						ЭтотОбъект[стр.Ключ] = стр.Значение;
					Исключение;	
					КонецПопытки
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	Исключение	
	КонецПопытки;
КонецПроцедуры

Процедура СохранитьНастройки() Экспорт
	
	настройки = Новый Структура;
	Для каждого стр из Метаданные().Реквизиты Цикл
		настройки.Вставить(стр.Имя, ЭтотОбъект[стр.Имя]);		
	КонецЦикла;
	табл = Новый Массив;
	настройки.Вставить("ИсключатьПапкиТоваров", табл);
	Для каждого стр из ИсключатьПапкиТоваров Цикл
		табл.Добавить(стр.Папка);
	КонецЦикла;
	
    зн = Новый ХранилищеЗначения(настройки);
	спр = Справочники.ДополнительныеОтчетыИОбработки.НайтиПоРеквизиту("ИмяОбъекта", "ВыгрузкаНаполеон");
	Если не спр.Пустая() Тогда
		спр = спр.ПолучитьОбъект();
		спр.ХранилищеНастроек = зн;
		спр.Записать();
	Иначе	
		ХранилищеОбщихНастроек.Сохранить("NapoleonExchange", "NapoleonExchange", настройки);
	КонецЕсли;
КонецПроцедуры

#Область ВыгрузкаДолгов

Функция ПолучитьЗагруженныеЗаявки(ДатаС)
	
	Рез = Новый Соответствие;
	
	фильтр = """type"" = 'Order' and ""created"" >= ToDate('" + Формат(ДатаС, "ДФ=дд/ММ/гггг") + "')";
	Для Каждого стр из ПолучитьДанные("DocCommitted", фильтр) Цикл
		ключ = стр.date + "|" + стр.number;
		рез.Вставить(ключ, Новый Структура("userid,created", стр.userid, стр.created));
	КонецЦикла;
	
	Возврат Рез;
КонецФункции

Процедура ВыгрузитьНакладные() ЭКСПОРТ
	накладные = Новый Массив;
	
	ДатаКнц = ТекущаяДата();
	ДатаНач = ДобавитьМесяц(ДатаКнц, -ГлубинаВыгрузкиДолгов);
	//Для Каждого стр из ПолучитьДанные("ExchangeInfo", """type""='Delivery'") Цикл
	//	ДатаНач = Дата(стр.time);
	//КонецЦикла;
	
	// загружаем заявкм на день раньше до накладных
	Заявки = ПолучитьЗагруженныеЗаявки(ДатаНач - 24 * 3600);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	РеализацияТоваровУслуг.Ссылка КАК Документ
		|ИЗ
		|	Документ.РасходнаяНакладная КАК РеализацияТоваровУслуг
		|ГДЕ
		|	РеализацияТоваровУслуг.Дата МЕЖДУ &ДатаНач И &ДатаКнц
		|	И РеализацияТоваровУслуг.ПометкаУдаления = ЛОЖЬ"
		;
	Запрос.УстановитьПараметр("ДатаНач", ДатаНач);
	Запрос.УстановитьПараметр("ДатаКнц", ДатаКнц);
	выб = Запрос.Выполнить().Выбрать();
	Пока выб.Следующий() Цикл
		
		заявкаАгента = Неопределено;
		Если ЗначениеЗаполнено(выб.Документ.Заказ) Тогда
			заказ = выб.Документ.Заказ;
			ключ = ДатаВСтроку(заказ.Дата) + "|" + СокрЛП(Заказ.Номер);
			заявкаАгента = Заявки.Получить(ключ);
		КонецЕсли;
		
		товары = Новый Массив;
		
		Для Каждого стр из выб.Документ.Запасы Цикл
			товары.Добавить(Новый Структура("id,qty,sum"
				,КодОбъекта(стр.Номенклатура)
				,стр.Количество
				,стр.Сумма
			));
		КонецЦикла;
		
		док = новый Структура("id,date,number,items"
			,КодОбъекта(выб.Документ.Контрагент)
			,ДатаВСтроку(выб.Документ.Дата)
			,выб.Документ.Номер
			,товары
		);
		Если заявкаАгента <> Неопределено Тогда
			док.Вставить("created", заявкаАгента.created);
			док.Вставить("userid", заявкаАгента.userid);
		КонецЕсли;
		накладные.Добавить(док);
	КонецЦикла;
	
	данные = Новый Массив;
	Если накладные.Количество() > 0 Тогда
		данные.Добавить(Новый Структура("Тип,Данные", "Delivery", накладные));
	КонецЕсли;
	
	// запишем информацию о времени выгрузки накладных
	данные.Добавить(Новый Структура("Тип,Данные"
		,"ExchangeInfo"
		,Новый Структура(
			"type,time"
			,"Delivery"
			,ДатаВСтроку(ДатаКнц)
		)
	));	
	
	ДобавитьНаСервер(данные,);
КонецПроцедуры

Процедура ВыгрузкаДолгов() Экспорт
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	РасчетыСПокупателямиОстатки.Контрагент КАК Контрагент,
		|	РасчетыСПокупателямиОстатки.СуммаОстаток КАК СуммаДолга,
		|	РасчетыСПокупателямиОстатки.Документ КАК Документ,
		|	ДОБАВИТЬКДАТЕ(РасчетыСПокупателямиОстатки.Документ.Дата, ДЕНЬ, РасчетыСПокупателямиОстатки.Договор.СрокОплатыПокупателя) КАК СрокОплаты,
		|	РасчетыСПокупателямиОстатки.ТипРасчетов КАК ТипРасчетов,
		|	ВложенныйЗапрос.СуммаОстаток КАК СуммаОстаток
		|ИЗ
		|	РегистрНакопления.РасчетыСПокупателями.Остатки КАК РасчетыСПокупателямиОстатки
		|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			СУММА(РасчетыСПокупателямиОстатки.СуммаОстаток) КАК СуммаОстаток,
		|			РасчетыСПокупателямиОстатки.Контрагент КАК Контрагент
		|		ИЗ
		|			РегистрНакопления.РасчетыСПокупателями.Остатки КАК РасчетыСПокупателямиОстатки
		|		ГДЕ
		|			ТИПЗНАЧЕНИЯ(РасчетыСПокупателямиОстатки.Документ) = ТИП(Документ.РасходнаяНакладная)
		|		
		|		СГРУППИРОВАТЬ ПО
		|			РасчетыСПокупателямиОстатки.Контрагент) КАК ВложенныйЗапрос
		|		ПО РасчетыСПокупателямиОстатки.Контрагент = ВложенныйЗапрос.Контрагент
		|ГДЕ
		|	РасчетыСПокупателямиОстатки.Контрагент.Ответственный <> ЗНАЧЕНИЕ(Справочник.Сотрудники.ПустаяСсылка)
		|	И ТИПЗНАЧЕНИЯ(РасчетыСПокупателямиОстатки.Документ) = ТИП(Документ.РасходнаяНакладная)
		|
		|УПОРЯДОЧИТЬ ПО
		|	Контрагент";

	текКА = Неопределено;
	Результат = Запрос.Выполнить();
	Выборка = Результат.Выбрать(ОбходРезультатаЗапроса.Прямой);

	долги = новый Массив;
	долгиПоДокументам = Неопределено;
	
	// текущая реализация выгружает только накладные
	типДокумента = "Delivery";
	заголовок = "Реализация";
	
	Пока Выборка.Следующий() Цикл
		Если текКА <> Выборка.Контрагент Тогда
			текКА = Выборка.Контрагент;
			
			долгиПоДокументам = Новый Массив;
			дд = Новый Структура("id,sum,documents"
				,КодОбъекта(текКА)
				,выборка.СуммаОстаток
				,долгиПоДокументам
			);
			долги.Добавить(дд);
		КонецЕсли;
		
		док = новый Структура("date,sum,sumDoc,payDate,number,type,title"
			,ДатаВСтроку(Выборка.Документ.Дата)
			,выборка.СуммаДолга
			,выборка.Документ.СуммаДокумента
			,ДатаВСтроку(Выборка.СрокОплаты)
			,выборка.Документ.Номер
			,типДокумента
			,заголовок
		);
		долгиПоДокументам.Добавить(док);		
	КонецЦикла;

	ДобавитьНаСервер(долги, "Balance");

	ВыгрузитьНакладные();
КонецПроцедуры

#КонецОбласти
